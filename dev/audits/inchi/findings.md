# InChI static audit findings

Three hundred thirty-eight production functions audited; thirty-six source-supported behavior/performance findings recorded. Stable IDs begin at `INCHI-AUDIT-0001`. Record each concrete possible difference here before inserting its direct panic marker.

Progress: **411/2019 production functions audited; 71 findings; 109 inserted panics**. A finding must identify source-defined behavior or a concrete source-supported cost regression, distinguish suspicion from static confirmation, and preserve the precise branch and affected callers. The approved deterministic allocation error for the source-undefined `NormalizeAndCompare` null-copy path is not a divergence finding. Missing source evidence remains in the inventory until resolved.

At initial discovery, INCHI-AUDIT-0001 applied to these already-audited pointer-bearing generated defaults (including nested pointers): INCHI-F1822, INCHI-F1823, INCHI-F1827, INCHI-F1830, INCHI-F1831, INCHI-F1832, INCHI-F1833, INCHI-F1834, INCHI-F1835, INCHI-F1839, INCHI-F1840, INCHI-F1841, INCHI-F1843, INCHI-F1844, INCHI-F1846, INCHI-F1847, INCHI-F1848, INCHI-F1850, INCHI-F1851, INCHI-F1853, INCHI-F1854, INCHI-F1855, INCHI-F1863, INCHI-F1864, INCHI-F1866, INCHI-F1867, INCHI-F1870, INCHI-F1871, INCHI-F1872, INCHI-F1873, INCHI-F1875, INCHI-F1879, INCHI-F1885, INCHI-F1887, INCHI-F1890, INCHI-F1895, INCHI-F1896. This is one shared representation finding; later affected audited functions are marked in inventory.md.

| ID | Function | Axis | Static confidence | Pinned source evidence | Rust evidence | Trigger and expected/actual state or cost | Affected callers | Panic site |
|---|---|---|---|---|---|---|---|---|
| INCHI-AUDIT-0001 | INCHI-F1896 under audit; shared `SourceMutPointer::default` INCHI-F2012 and generated pointer-bearing defaults including INCHI-F1822 | Performance: per-pointer state, struct footprint, zeroing/hot-path bandwidth | Static state expansion confirmed; hot-path effect source-supported suspicion, unmeasured | Pinned C `third_party/InChI/INCHI-1-SRC/INCHI_BASE/src/ichicant.h:149..188` declares native pointer fields in `FTCN` (e.g. `NEIGH_LIST *NeighList`, `AT_RANK *LinearCt`); C `mode.h:1034..1040` has a single `char *pStr` in `tagOutputString`. Under configured GCC/Linux LP64 each native pointer is 8 bytes. | Rust `crates/cosmolkit-inchi/src/source_types/mod.rs:72..84` defines `AllocationId(u64)` and each source pointer as `Option<AllocationId>` plus a separate `u64 element_offset`; `source_types/generated.rs:3086..3139` uses many such pointer fields in `FTCN`, and `generated.rs:926..940` uses one in `tagOutputString`. | Every default-created Rust source pointer carries two 64-bit payloads plus an option tag instead of one C pointer; pointer-bearing structs require more storage and initialization traffic. This is a concrete footprint regression relative to upstream; cache/memory-bandwidth cost in pointer-heavy paths is suspected from source shape, not measured. No behavioral difference is claimed. | All pointer-bearing generated defaults and callers of `SourceMutPointer::default` or `SourceConstPointer::default`; direct constructors must be checked independently. Previously recorded no-finding cost conclusions for such defaults are superseded on the footprint axis. | Direct `panic!` at `crates/cosmolkit-inchi/src/source_types/mod.rs:118` in the shared pointer-value `Default` macro; no branch exists, so quarantine covers both mutable and const pointer defaults. Original `Self::null()` remains intact below it. |
| INCHI-AUDIT-0002 | INCHI-F2017 and INCHI-F2018, shared checked pointer `offset` | Performance: repeated checked pointer advances in source parse loop | Concrete extra per-advance operations confirmed statically; net elapsed cost suspected, unmeasured | Pinned C `third_party/InChI/INCHI-1-SRC/INCHI_BASE/src/ichiread.c:4451..4475` uses native `p = q + (*q == ',')` three times and `p++` once per coordinate; these are direct pointer advances inside `AddAuxSegmentXYZ`; `INCHI_BASE/src/ichisort.c:869..885` uses `pAtList + start` once per atom in `CreateNeighList`. | Rust `crates/cosmolkit-inchi/src/source_types/mod.rs:136..149` computes unsigned magnitude, branches by sign, checks add/sub overflow and propagates `Result`; `source/base/ichiread.rs:9081,9105,9115` calls it in the matching coordinate loop; `source/base/ichisort.rs:2055..2077` calls it once per atom. | For each coordinate, at least four valid pointer advances take the Rust checked/error path where pinned C performs direct pointer arithmetic. Time remains O(n), space/allocation unchanged, but additional hot-loop branches and error propagation are a concrete possible cost regression on valid source inputs. Compiler elimination of these checks and end-to-end magnitude cannot be verified under the no-execution constraint. No behavioral difference is claimed for valid in-bounds pointers. | Every caller of `SourceConstPointer::offset` or `SourceMutPointer::offset`; `AddAuxSegmentXYZ` is a pinned-source loop witness. | Direct `panic!` at `crates/cosmolkit-inchi/src/source_types/mod.rs:138` in the shared `offset` macro body, preserving the existing checked implementation below. |
| INCHI-AUDIT-0003 | INCHI-F2025, `SourceMutPointer::difference` | Performance: repeated guarded pointer subtraction on bond-network edge construction | Concrete extra checks confirmed statically; compiled cost and net elapsed impact unmeasured | Pinned C `third_party/InChI/INCHI-1-SRC/INCHI_BASE/src/ichi_bns.c:6555..6578` computes native `p1->iedge - pBNS->iedge` and `p2->iedge - pBNS->iedge` inside `AddNewEdge`, with direct comparisons against adjacency limits. | Rust `crates/cosmolkit-inchi/src/source_types/mod.rs:205..219` first unwraps allocation identity, checks origin identity, branches on offset order, converts the magnitude with overflow handling, then returns `Result`; `source/base/ichi_bns.rs:7322..7333` calls it twice in the matching edge path. | On valid same-allocation edge pointers, each subtraction adds identity/order/conversion/error branches beyond source native pointer subtraction. Both stay O(1), with no new allocation, but per-edge hot-path work is a concrete possible performance regression. Whether optimization removes these checks is unresolved under the no-execution constraint. No behavioral difference is claimed for valid pointers; C subtraction across unrelated objects is not a modeled valid case. | All callers of `SourceMutPointer::difference`, including the two per-edge `AddNewEdge` calls; const-pointer callers convert through `as_mut`. | Direct `panic!` at `crates/cosmolkit-inchi/src/source_types/mod.rs:207` in `SourceMutPointer::difference`, preserving the checked implementation below. |
| INCHI-AUDIT-0004 | INCHI-F2047 `AllocationArena::insert`, discovered while reviewing INCHI-F2029 storage ownership | Performance: retained arena slot metadata grows with total allocation history | Static asymptotic space regression confirmed | Pinned C `third_party/InChI/INCHI-1-SRC/INCHI_BASE/src/mode.h:1169..1181` maps `inchi_malloc`/`inchi_calloc` to libc `malloc`/`calloc` and `inchi_free` to `free` under the configured non-`TARGET_EXE_USING_API` path; `INCHI_BASE/src/ichi_io.c:502..513` allocates and frees a replaced output buffer. | Rust `crates/cosmolkit-inchi/src/source_types/mod.rs:516..529` appends one `Option<AllocationSlot>` for every new ID, `:541..545` replaces freed entries with `None` but never shrinks/reuses them, and `:813..829` advances `next_id` monotonically. | Across N allocate/free cycles in one `SourceHeap`, C has no source-level retained allocation-history table after `free`; the Rust arena retains O(N) slot capacity and metadata even when live count returns to zero. Every new insertion may reserve more arena capacity. This is a concrete space and allocation-history cost regression; no chemistry behavior difference is claimed. | Every `SourceHeap::allocate`/`allocate_model_storage` caller, especially paths that repeatedly replace buffers or create temporary allocations. | Direct `panic!` in `AllocationArena::insert` at `crates/cosmolkit-inchi/src/source_types/mod.rs:530` before the slot-append path, preserving existing insertion below. |
| INCHI-AUDIT-0005 | INCHI-F2030, `AllocationSlot::downcast_ref` | Performance: dynamic type check on repeated source reads | Concrete per-read guard confirmed statically; net compiled cost unmeasured | Pinned C `third_party/InChI/INCHI-1-SRC/INCHI_BASE/src/ichiread.c:4465..4475` reads `*p` and `*q` directly inside the coordinate loop. | Rust `crates/cosmolkit-inchi/src/source_types/mod.rs:334..342` compares stored `TypeId` to `TypeId::of::<Vec<T>>()` before returning a slice; `:974..989` calls it on `SourceHeap::slice`, and `source/base/ichiread.rs:9084..9103` repeatedly requests slices in the matching loop. | For valid correctly typed buffers, each repeated read incurs an arena/type guard in addition to C's direct pointer dereference. The guard is constant time with no allocation, but its repeated hot-loop cost is a concrete possible regression; compiler elimination is unresolved without execution/compilation. No behavior difference is claimed for these typed source inputs. | All `SourceHeap::slice` and `stable_slice` callers using `AllocationSlot::downcast_ref`; the coordinate parser is an active loop witness. | Direct `panic!` at `crates/cosmolkit-inchi/src/source_types/mod.rs:336` at the dynamic type-check entry of `AllocationSlot::downcast_ref`, preserving the original check/body below. |
| INCHI-AUDIT-0006 | INCHI-F2031, `AllocationSlot::downcast_mut` | Performance: dynamic type check and provenance write on repeated source mutations | Concrete extra per-write guard/state store confirmed statically; net compiled cost unmeasured | Pinned C `third_party/InChI/INCHI-1-SRC/INCHI_BASE/src/ichisort.c:869..885` writes neighbor-list elements directly as `pAtList[length++]` inside each atom's bond loop. | Rust `crates/cosmolkit-inchi/src/source_types/mod.rs:344..354` compares `TypeId` and assigns `provenance = None` before returning a mutable slice; `:993..1009` calls it through `SourceHeap::slice_mut`, while `source/base/ichisort.rs:2055..2077` requests `heap.slice_mut(atom_list)` on each bond in the non-proved branch. | Valid typed writes still take repeated arena/type checks and provenance stores instead of direct C indexed writes. Both are linear over bonds with no new data allocation, but this adds hot-loop work and may invalidate a source-layout proof. The branch is concrete even though another proved fast path exists; compiler removal and elapsed magnitude remain unmeasured. No behavior difference is claimed for the witnessed typed writes. | All `SourceHeap::slice_mut` and other `downcast_mut` callers; `CreateNeighList` non-proved branch is an active witness. | Direct `panic!` at `crates/cosmolkit-inchi/src/source_types/mod.rs:347` at the dynamic type-check entry of `AllocationSlot::downcast_mut`, preserving the original check and provenance write below. |
| INCHI-AUDIT-0007 | INCHI-F2036, `StableSourceConstSlice::get` | Performance: per-element checked read in rank tie loops | Extra bounds/error branch confirmed statically; net compiled impact unmeasured | Pinned C `third_party/InChI/INCHI-1-SRC/INCHI_BASE/src/ichimap2.c:703..718` indexes rank and atom-number arrays directly in both tie-count loops. | Rust `crates/cosmolkit-inchi/src/source_types/mod.rs:402..410` checks `index >= len` and returns `Result` before each read; `source/base/ichimap2.rs:179..200` calls it twice per tie-loop iteration after stable view construction. | On valid rank indices, repeated bounds/error branches add hot-loop work beyond direct C indexed reads. Time stays O(number of ties) and no allocation is introduced; optimization of checks is unverified. This is a concrete possible performance regression, with no behavior difference claimed for valid indices. | All `StableSourceConstSlice::get` callers, including `NumberOfTies` tie scans. | Direct `panic!` at `crates/cosmolkit-inchi/src/source_types/mod.rs:405` at the bounds-check entry of `StableSourceConstSlice::get`, preserving the original branch/body. |
| INCHI-AUDIT-0008 | INCHI-F2040, `StableSourceSlice::get` | Performance: per-element checked read in bond-network loops | Extra bounds/error branch confirmed statically; net compiled impact unmeasured | Pinned C `third_party/InChI/INCHI-1-SRC/INCHI_BASE/src/ichi_bns.c:9264..9295,9334..9353` indexes vertices and incident edges directly while adding tautomer-group vertices and scanning endpoint edges. | Rust `crates/cosmolkit-inchi/src/source_types/mod.rs:444..454` checks `index >= len` and returns `Result`; `source/base/ichi_bns.rs:18587..18649` calls it inside matching group and endpoint loops. | The valid-index path gains a repeated branch/error edge where C performs direct indexed access. Both remain linear in visited vertices/edges without allocation; material elapsed effect is suspected, not measured. | All `StableSourceSlice::get` callers, with `AddTGroups2BnStruct` as a pinned loop witness. | Direct `panic!` at `crates/cosmolkit-inchi/src/source_types/mod.rs:447` at the bounds-check entry of `StableSourceSlice::get`, preserving original logic. |
| INCHI-AUDIT-0009 | INCHI-F2041, `StableSourceSlice::get_mut` | Performance: per-element checked write in rank and bond-network loops | Extra bounds/error branch confirmed statically; net compiled impact unmeasured | Pinned C `third_party/InChI/INCHI-1-SRC/INCHI_BASE/src/ichi_bns.c:9267..9280,9334..9353` writes vertices and edge capacities directly; `INCHI_BASE/src/ichimap2.c:263..294` bulk-zeros then writes rank arrays through typed pointers. | Rust `crates/cosmolkit-inchi/src/source_types/mod.rs:456..466` checks `index >= len` and returns `Result` before mutable access; `source/base/ichi_bns.rs:18587..18598` and `source/base/ichimap2.rs:1609..1675` call it in corresponding loops; Rust clears ranks element-by-element at `ichimap2.rs:1610..1616` where C uses `memset`. | Every valid checked write adds a branch/error path beyond C direct stores. Time stays linear with no extra allocation; compiler elimination and net effect remain unmeasured, making this a concrete possible hot-path regression. | All `StableSourceSlice::get_mut` callers in source loops. | Direct `panic!` at `crates/cosmolkit-inchi/src/source_types/mod.rs:459` at the bounds-check entry of `StableSourceSlice::get_mut`, preserving original logic. |
| INCHI-AUDIT-0010 | INCHI-F2092, INCHI-F2054 and INCHI-F2055, `copy_inp_atom_gcc_lp64_byte_prefix` and its field codecs | Performance: full atom encode/decode around partial C byte copy | Static extra memory traffic and field conversion confirmed; elapsed magnitude unmeasured | Pinned C `third_party/InChI/INCHI-1-SRC/INCHI_BASE/src/ichinorm.c:5918..5924` executes `memcpy(at + num_atoms, at_H, num_added_explicit_H * sizeof(at))`; configured LP64 makes `sizeof(at)` 8 bytes. `INCHI_BASE/src/inpdef.h:141..198` declares the 176-byte `inp_ATOM` layout, and `mode.h:534` activates `UNDERIV_ADD_EXPLICIT_H`. | Rust `crates/cosmolkit-inchi/src/source_types/mod.rs:592..726` serializes every touched source and destination atom to separate 176-byte arrays, copies only the requested prefix, then deserializes all fields of each touched destination atom; the sole production caller is `source/base/ichinorm.rs:718..732`. | For N added atoms, C copies 8N contiguous bytes. Rust touches at least three 176-byte atom representations for each of ceil(8N/176) touched atoms. Asymptotic time remains O(N), but the extra full-atom traffic and per-field conversions are a concrete performance regression on the active source branch. Behavior for named fields is analyzed separately; padding bytes are not modeled. | `add_explicit_H` through `copy_inp_atom_gcc_lp64_byte_prefix`, plus the two field codec helpers; no other production caller was found. | Direct `panic!` at `crates/cosmolkit-inchi/src/source_types/mod.rs:712` at the start of `copy_inp_atom_gcc_lp64_byte_prefix`, preserving the original code. |
| INCHI-AUDIT-0011 | INCHI-F0606, `add_explicit_H`, discovered while reviewing its INCHI-F2055 helper | Performance: extra full-atom temporary clone before source partial byte copy | Extra O(N) allocation, copy and peak buffer confirmed statically | Pinned C `third_party/InChI/INCHI-1-SRC/INCHI_BASE/src/ichinorm.c:5918..5924` copies only `num_added_explicit_H * sizeof(at)` bytes directly from the already allocated `at_H` buffer to `at + num_atoms`; it does not create an intervening full-atom clone. | Rust `crates/cosmolkit-inchi/src/source/base/ichinorm.rs:718..732` copies the entire prefix of N `inp_ATOM` values with `.to_vec()` into `temporary_atoms` before passing it to `copy_inp_atom_gcc_lp64_byte_prefix`; each modeled atom has the pinned 176-byte C shape in `inpdef.h:141..198`. | On N added atoms, Rust allocates and copies N full atoms before the partial byte-prefix copy, adding O(176N) transient storage/traffic where C uses the existing source buffer and copies 8N bytes under LP64. This is independent of the field-codec overhead in INCHI-AUDIT-0010. The values copied may still match; this finding is performance only. | The active `UNDERIV_ADD_EXPLICIT_H` branch in `add_explicit_H` and its callers. | Direct `panic!` at `crates/cosmolkit-inchi/src/source/base/ichinorm.rs:719` immediately before the `.to_vec()` expression, preserving that expression and subsequent logic. |
| INCHI-AUDIT-0012 | INCHI-F1635, `inchi_malloc`, discovered while reviewing `SourceHeap::allocate` | Performance: full zero-fill of source malloc storage | Static extra O(n) write confirmed; elapsed impact unmeasured | Pinned C `third_party/InChI/INCHI-1-SRC/INCHI_BASE/src/mode.h:1169..1181` maps `inchi_malloc` directly to libc `malloc` in this profile; `INCHI_BASE/src/ichiprt1.c:2956` allocates a coordinate-output string with it before writing output. | Rust `crates/cosmolkit-inchi/src/source/base/util.rs:56..64` reserves `byte_count` bytes and calls `bytes.resize(byte_count, 0_i8)` before `heap.allocate`; `source/base/ichiprt1.rs:12131..12150` uses this wrapper for the matching coordinate-output buffer. | On a valid allocation of n bytes, Rust explicitly writes all n bytes before source output fills them, where pinned C's `malloc` has no source-level zero-fill. This adds O(n) memory traffic and a full initialization pass on the output allocation path, independent of the arena-history finding INCHI-AUDIT-0004. No output-value difference is claimed when the source writes the used bytes. | All production `inchi_malloc` callers, including output and temporary byte buffers. | Direct `panic!` at `crates/cosmolkit-inchi/src/source/base/util.rs:62` immediately before `bytes.resize(byte_count, 0_i8)`, preserving the existing initialization below. |
| INCHI-AUDIT-0013 | INCHI-F0542, `ContiguousNeighborRankSortWorkspace::try_new`, discovered during INCHI-F2062/F2063 proof review | Performance: extra complete index-bound scan when provenance is absent | Static O(n) setup pass confirmed for the unproved branch; production frequency unmeasured | Pinned C `third_party/InChI/INCHI-1-SRC/INCHI_BASE/src/ichimap2.c:703..718,241..299` directly indexes atom-number/rank arrays in tie and rank-refinement loops without a preliminary full-array validation pass. | Rust `crates/cosmolkit-inchi/src/source/base/ichimap2.rs:803..845` calls `has_proven_index_bound`; if false, `.prefix(count)?.iter().any(|&atom| usize::from(atom) >= count)` scans the entire index prefix before work and records the proof; production callers include `SortNeighLists2/3` at `ichimap2.rs:2161..2169,2229..2237`. | A valid source-built array with no recorded provenance triggers an additional O(n) read pass and hot buffer traffic before the C-equivalent algorithm. Proof reuse can amortize later calls, but the first call still pays a source-absent scan. No behavior difference is claimed for valid bounded indices. | `SortNeighLists2/3` and other `try_new` callers when their atom-number allocation lacks a stored bound. | Direct `panic!` in the `!atom_numbers_are_bounded` branch just before the scan in `crates/cosmolkit-inchi/src/source/base/ichimap2.rs`, preserving the original scan and fallback below. |
| INCHI-AUDIT-0014 | INCHI-F2071, `SourceHeap::with_two_slices_mut_and_optional_third` | Performance: per-edge multi-allocation extraction and restoration | Static extra arena operations confirmed; elapsed impact unmeasured | Pinned C `third_party/InChI/INCHI-1-SRC/INCHI_BASE/src/ichirvr1.c:2627..2665` connects an edge by direct access to existing vertex, edge and incident-edge buffers in `ConnectTwoVertices`. | Rust `crates/cosmolkit-inchi/src/source_types/mod.rs:1150..1223` checks three allocation identities, removes up to three arena slots, type-checks/bounds each, calls the body, then restores each slot; `source/base/ichirvr1.rs:6596..6626` invokes it for the corresponding edge connection. | For each valid edge connection, Rust performs up to six arena slot state transitions and multiple dynamic checks before/after direct element work, where C retains raw pointers and writes directly. Time is still O(1) per edge with no extra data allocation, but this adds concrete per-edge hot-path work and cache traffic. The allocation-history issue INCHI-AUDIT-0004 is separate. | `ConnectTwoVertices` and other callers of this three-slice helper. | Direct `panic!` at the start of `with_two_slices_mut_and_optional_third` in `crates/cosmolkit-inchi/src/source_types/mod.rs`, preserving the original ownership/alias logic below. |
| INCHI-AUDIT-0015 | INCHI-F1637, `inchi_realloc` non-null branch | Performance: duplicate full old-buffer clone before replacement | Static additional O(old) allocation and copy confirmed; elapsed impact unmeasured | Pinned C `third_party/InChI/INCHI-1-SRC/INCHI_BASE/src/mode.h:1177..1178` calls libc `realloc` directly, with non-null buffer caller `INCHI_BASE/src/ichiread.c:8287..8291`; it passes the existing buffer without first cloning it. | Rust `crates/cosmolkit-inchi/src/source/base/util.rs:111..115` calls `heap.slice(...)? .to_vec()` for every non-null input, then `:116..129` allocates a replacement, copies the cloned prefix and frees the old buffer; `source/base/ichiread.rs:14977..14988,15050` uses this wrapper. | For a valid old buffer of n elements, Rust holds a separate n-element clone in addition to the original and replacement, with an extra full copy and peak O(n) storage that are absent from the source-level `realloc` call. Whether libc itself relocates storage is independent of this additional clone. | All production non-null `inchi_realloc` callers. | Direct `panic!` immediately before `.to_vec()` in the non-null branch of `crates/cosmolkit-inchi/src/source/base/util.rs`, retaining that expression below. |
| INCHI-AUDIT-0016 | INCHI-F1637, `inchi_realloc` replacement initialization | Performance: unconditional full replacement zero-initialization | Static additional O(new) write pass confirmed; elapsed impact unmeasured | Pinned C `third_party/InChI/INCHI-1-SRC/INCHI_BASE/src/mode.h:1177..1178` maps to libc `realloc`; its newly added tail is not initialized by the source call, and `INCHI_BASE/src/ichiread.c:8287..8291` invokes it for an existing S_CHAR array. | Rust `crates/cosmolkit-inchi/src/source/base/util.rs:116..123` reserves a new vector and calls `replacement.resize(count, T::default())` before copying the preserved prefix; production callers include `source/base/ichiread.rs:15050` and `source/base/ichirvr4.rs:9051`. | On a successful request for m elements, Rust initializes all m replacement elements, then overwrites up to min(old,m) of them. This adds a full initialization pass and can invoke aggregate `Clone` for each element where pinned C specifies only preserved prefix bytes and indeterminate new tail. No valid-output difference is claimed for subsequent initialized uses. | All production `inchi_realloc` callers, including a null input for which C `realloc(NULL,size)` acts as malloc. | Direct `panic!` immediately before `replacement.resize` in `crates/cosmolkit-inchi/src/source/base/util.rs`, retaining the initialization below. |
| INCHI-AUDIT-0017 | INCHI-F1649, `mystrncpy`, discovered while reviewing INCHI-F1641 `read_upto_delim` | Performance: heap temporary on every source string copy | Static additional O(copied length) allocation, copy and peak storage confirmed | Pinned C `third_party/InChI/INCHI-1-SRC/INCHI_BASE/src/util.c:1760..1798` scans source length, then directly `memmove`s into the caller's target and `memset`s the tail; `util.c:1682..1688` calls it for delimiter fields. | Rust `crates/cosmolkit-inchi/src/source/base/util.rs:951..980` builds a new Vec with `try_reserve_exact`, copies the source prefix, appends NUL, then copies that temporary into the target via `mystrncpy_slice`; production callers include `source/base/util.rs:381..387` and `source/base/mol_fmt4.rs:592..614`. | Each valid copy of n bytes now allocates n+1 extra bytes and copies the prefix into that temporary before the destination copy. C's `memmove` already handles overlap without a separate heap buffer. This adds allocation and memory traffic in parsing/name-copy paths while keeping the same O(n) asymptotic time; no behavior difference is claimed for reviewed valid buffers. | `read_upto_delim` and other production `mystrncpy` callers. | Direct `panic!` immediately before `let mut copied = Vec::new()` in `crates/cosmolkit-inchi/src/source/base/util.rs`, preserving allocation/copy code below. |
| INCHI-AUDIT-0018 | INCHI-F1647, `extract_inchi_substring` | Performance: temporary full substring clone before destination copy | Static additional O(substring length) allocation and copy confirmed | Pinned C `third_party/InChI/INCHI-1-SRC/INCHI_BASE/src/util.c:1860..1917` scans for `InChI=`, allocates exactly `i+1` output bytes and `memcpy`s directly from `p` into that output. | Rust `crates/cosmolkit-inchi/src/source/base/util.rs:737..785` finds the prefix and valid span, allocates the destination, then calls `.to_vec()` on the source substring before `copy_from_slice`; production caller `source/base/runichi.rs:2687` invokes this path. | For a valid substring of n bytes, Rust creates an additional n-byte Vec and reads/copies all n bytes twice before completing the destination, adding O(n) transient space and one extra allocation absent from C. | Production `extract_inchi_substring` callers in `runichi.rs`. | Direct `panic!` immediately before the `.to_vec()` source-substring expression in `crates/cosmolkit-inchi/src/source/base/util.rs`, preserving the copy below. |
| INCHI-AUDIT-0019 | INCHI-F1648, `extract_auxinfo_substring` | Performance: temporary full AuxInfo clone before destination copy | Static additional O(substring length) allocation and copy confirmed | Pinned C `third_party/InChI/INCHI-1-SRC/INCHI_BASE/src/util.c:1921..1958` scans for `AuxInfo=`, allocates `i+1` output bytes and `memcpy`s directly from the located source pointer. | Rust `crates/cosmolkit-inchi/src/source/base/util.rs:844..897` scans to source whitespace, then calls `.to_vec()` on the entire matched substring before allocating and copying into the result; production caller `source/base/runichi.rs:2696` uses it. | For each valid n-byte AuxInfo result, Rust allocates a separate n-byte Vec and copies the bytes into it before the source-defined destination allocation/copy. This adds O(n) peak storage, one allocation and one full copy. | Production `extract_auxinfo_substring` callers in `runichi.rs`. | Direct `panic!` immediately before the source-substring `.to_vec()` in `crates/cosmolkit-inchi/src/source/base/util.rs`, retaining the existing clone/copy below. |
| INCHI-AUDIT-0020 | INCHI-F1652, `source_is_ascii_lower`, used by pending INCHI-F1662 `extract_H_atoms` | Behavior: high-bit byte after an H/D/T alias character becomes an error | Source branch divergence confirmed; reachability from arbitrary molfile alias bytes source-supported | Pinned C `third_party/InChI/INCHI-1-SRC/INCHI_BASE/src/util.c:774..811` reads the next alias byte as `UCINT q[0]` and passes the resulting 0..255 value to `islower`; `:811..842` continues either hydrogen extraction or ordinary scan, without an encoding error. `INCHI_BASE/src/mol2atom.c:598` calls this helper for molfile atom aliases. | Rust `crates/cosmolkit-inchi/src/source/base/util.rs:1019..1025` returns `InvalidSourceTextEncoding` for any byte >=0x80; `:2983..2989` invokes it when the current alias character is H/D/T, before the source-defined branch can continue. | For a NUL-terminated alias such as `H\xE9` entering this source path, C obtains a Boolean ctype result and continues the loop, while Rust returns an error. The exact Boolean depends on C locale; neither source branch is an encoding error. No claim is made that the resulting alias is a recognized element. | `extract_H_atoms` via molfile atom alias handling and any other caller of this helper. | Direct `panic!` inside the `!value.is_ascii()` branch of `crates/cosmolkit-inchi/src/source/base/util.rs`, preserving the original error return below. |
| INCHI-AUDIT-0021 | INCHI-F1655, `if_skip_add_H` table lookup | Behavior: eight active element flags differ from pinned `ElData` | Exact static table-row mismatch confirmed | Pinned C `third_party/InChI/INCHI-1-SRC/INCHI_BASE/src/util.c:189..196` gives `bSkipAddingH=0` for Tl, Pb, Bi, Po, At, Rn, Fr and Ra (active `ElData` indices 82..89); `util.c:428..433` returns those flags for corresponding periodic numbers 81..88. | Rust `crates/cosmolkit-inchi/src/source/base/util.rs:1077..1096` sets the entire index range 58..121 to `true`, including 82..89; `:2138..2151` returns this projected flag. Callers include `source/base/util.rs:2471` and `source/base/ichirvr1.rs:657`. | For any of these eight valid elements, C reports no skip flag while Rust reports skip, potentially changing hydrogen-addition and reverse-structure decisions. The mismatch was checked across all 122 active rows: these eight are the only discrepancies in this flag projection. | `if_skip_add_H` callers for periodic numbers 81..88, including valence/hydrogen logic. | Direct `panic!` in a narrow `if (82..=89).contains(&table_index)` branch before `if_skip_add_H` lookup, and a second direct `panic!` before the independent `get_num_H` read of the same bad table in `crates/cosmolkit-inchi/src/source/base/util.rs`; the original table and both lookups remain intact. |
| INCHI-AUDIT-0022 | INCHI-F1664, `get_el_type` | Behavior: Er and Yb have exchanged `METAL` versus `METAL2` flags | Exact active-table mismatch confirmed | Pinned C `third_party/InChI/INCHI-1-SRC/INCHI_BASE/src/util.c:176,178` declares Er (active index 69) with `METAL=1` and Yb (index 71) with `METAL2=3`, as defined at `INCHI_BASE/src/incomdef.h:115..117`; `util.c:679..682` returns the whole `nType`. `INCHI_BASE/src/strutil.c:2519,2727` consumes the type. | Rust `crates/cosmolkit-inchi/src/source/base/util.rs:1054..1075` builds `EL_DATA_TYPES` from the correct metal Boolean but includes index 69 in its `metal2` list and omits index 71; `:3075..3101` returns the incorrect type. `source/base/strutil.rs:2773..2781` tests the type's 1/2 bits. | For Er (periodic 68), C returns 1 while Rust returns 3; for Yb (periodic 70), C returns 3 while Rust returns 1. Downstream valence/salt branches using the second metal bit can change, even though `is_el_a_metal` remains true for both. Static comparison of all 122 active type rows found only these two mismatches. | `get_el_type` callers including metal-salt and metal-bond classification. | Direct `panic!` in a narrow `if matches!(table_index, 69 | 71)` branch before the type-table lookup in `crates/cosmolkit-inchi/src/source/base/util.rs`, preserving the original projection. |
| INCHI-AUDIT-0023 | INCHI-F1665, `get_num_H` | Performance: four synchronized cache probes on every atom instead of plain static integer checks | Source-level extra atomic/cache-state work confirmed; optimized elapsed effect unmeasured | Pinned C `third_party/InChI/INCHI-1-SRC/INCHI_BASE/src/util.c:874..891` holds four function-static integer element numbers and checks each with `if (!intl_el_number_*)` on every call; `INCHI_BASE/src/mol2atom.c:958` invokes `get_num_H` in atom processing. | Rust `crates/cosmolkit-inchi/src/source/base/util.rs:3264..3282` uses four `OnceLock<i32>` values and calls `get_or_init` through `cached_element` four times before any branch, including aliased/explicit-valence early returns; `source/base/mol2atom.rs:1915` invokes it per atom. | After initialization, every atom still performs four synchronized OnceLock state probes where C performs four ordinary static integer loads/branches. Both are O(1) with no additional allocation, but the Rust per-atom hot-path atomic operations and cache traffic are a concrete possible performance regression. | All `get_num_H` callers, especially per-atom molfile conversion. | Direct `panic!` immediately before `slot.get_or_init` in the local `cached_element` closure inside `crates/cosmolkit-inchi/src/source/base/util.rs`, leaving the four cache calls and source logic below intact. |
| INCHI-AUDIT-0024 | INCHI-F1666, `num_of_H` | Performance: repeated checked neighbor and atom indexing in frequently called graph query | Concrete extra per-neighbor bounds/error paths confirmed statically; net elapsed effect unmeasured | Pinned C `third_party/InChI/INCHI-1-SRC/INCHI_BASE/src/util.c:1129..1142` directly indexes `a->neighbor[i]` and `at[n]` for each neighbor; `INCHI_BASE/src/strutil.c:1153,1220,1282..1304` calls this query within atom/bond scans. | Rust `crates/cosmolkit-inchi/src/source/base/util.rs:3384..3407` converts each loop index, calls `.get` on the fixed neighbor array, then `.get` on the atom slice and propagates errors; corresponding `source/base/strutil.rs:1422,1483,1527..1548` calls it repeatedly. | For each valid neighbor, Rust adds two checked Option/error paths and conversions beyond C direct array reads. Complexity remains O(valence), with no allocation, but these are concrete extra hot-path branches in a repeatedly invoked graph query. No valid-state result difference is claimed. | `num_of_H` callers in salt/tautomer and bond-network scans. | Direct `panic!` at the beginning of the per-neighbor loop body in `crates/cosmolkit-inchi/src/source/base/util.rs`, retaining the checked reads below. |
| INCHI-AUDIT-0025 | INCHI-F1674 `is_in_the_list` and INCHI-F1675 `is_in_the_ilist` | Performance: checked access on every neighbor probe | Per-iteration extra bounds/error path confirmed in source; net elapsed cost unmeasured | Pinned C `third_party/InChI/INCHI-1-SRC/INCHI_BASE/src/util.c:1059..1066` loops over `*pathAtom` with direct pointer increment; `INCHI_BASE/src/util.c:1072..1079` uses the same direct-pointer loop for integer lists; bond processing and `is_ilist_inside` invoke these helpers. | Rust `crates/cosmolkit-inchi/src/source/base/util.rs:3789..3821` runs an index loop and calls `path.get(index).ok_or(...)` for every candidate; `source/base/util.rs:3824..3856` repeats the checked loop for integer lists; production `source/api/inchi_dll.rs:4330..4348` calls it for reciprocal/duplicate bond checks, and `source/base/strutil.rs:1335` calls it in atom scans. | For each valid neighbor probe, Rust adds a dynamic bounds check and Option/error branch beyond the direct C pointer read. Both are O(path length), O(1) space and allocation-free, but the repeated checked access in bond and atom scans is a concrete possible hot-path slowdown. No valid-state result difference is claimed. | Both lookup helpers’ callers, including atom/bond processing, polymer list scans and output. | Direct `panic!` at the beginning of the existing `for index in 0..path_length` loop body of each helper in `crates/cosmolkit-inchi/src/source/base/util.rs`; each original checked read follows. |
| INCHI-AUDIT-0026 | INCHI-F1677 `n_bonds_val_to_metal` INCHI-F1678 `n_no_metal_num_bonds`, INCHI-F1679 `n_no_metal_bonds_valence`, and INCHI-F1680..F1682 neighbor-index helpers | Performance: repeated checked neighbor/atom/bond indexing inside atom processing | Extra loop operations confirmed statically; net elapsed cost unmeasured | Pinned C `third_party/InChI/INCHI-1-SRC/INCHI_BASE/src/util.c:1100..1125` directly reads `a->neighbor[i]`, `at[...]` and `a->bond_type[i]` in its valence loop; `INCHI_BASE/src/util.c:1253..1382` similarly reads neighbor/atom/bond fields directly in two active branches in each metal-bond helper; `INCHI_BASE/src/util.c:1386..1446` has the same direct-pointer neighbor walks in three neighbor-index helpers; `INCHI_BASE/src/mol2atom.c` calls the first helper during atom conversion. | Rust `crates/cosmolkit-inchi/src/source/base/util.rs:3901..3968` performs `usize::try_from`, `.get` on neighbor array, `.get` on atom slice and `.get` on bond array with separate error propagation inside the same loop; Rust `source/base/util.rs:3971..4126` repeats the checked reads in both metal-bond branch loops; `source/base/util.rs:4128..4279` repeats them in both branches of the third helper; `source/base/util.rs:4281..4435` repeats checked neighbor and atom reads in all three neighbor-index helpers; `source/base/strutil.rs:1407..2117` calls the metal-bond and index helpers repeatedly during atom scans. | For each valid neighbor, Rust adds three bounds/error branches and an index conversion to the source direct accesses. Both are O(valence), O(1) space and allocation-free, but repeated checks in the per-atom graph path are a concrete possible hot-path regression. Valid-state chemistry appears unchanged. | Both helpers’ callers in atom conversion, API bond processing and `strutil` atom scans. | Direct `panic!` at the beginning of the existing neighbor loop of `n_bonds_val_to_metal` and both active metal-bond loops in each of `n_no_metal_num_bonds` and `n_no_metal_bonds_valence`, plus the existing neighbor loop of each of the three neighbor-index helpers, preserving all checked reads below. |
| INCHI-AUDIT-0027 | INCHI-F1683 `inchi_memicmp` | Performance: two full heap pointer resolutions per compared byte | Repeated work confirmed statically; net elapsed cost unmeasured | Pinned C `third_party/InChI/INCHI-1-SRC/INCHI_BASE/src/util.c:1971..1991` advances two `U_CHAR*` pointers and loads each byte directly in its length loop. | Rust `crates/cosmolkit-inchi/src/source/base/util.rs:4445..4500` calls `heap.slice(first)?` and `heap.slice(second)?` inside every loop iteration before `.get(index)`; `source_types/mod.rs:982..999` shows each `slice` resolves allocation ID, slot, type and offset, then creates a suffix view. | For every compared byte, Rust repeats two arena slot lookups, type/provenance checks, offset conversions and slice bounds checks beyond C direct pointer loads. Both remain O(length) and allocation-free, but this is a concrete avoidable per-byte hot-path cost. Shared downcast-type finding INCHI-AUDIT-0005 covers one component; this finding covers the repeated full resolution. | All `inchi_memicmp` callers using the source heap. | Direct `panic!` inside the existing `while length != 0` loop before the two `heap.slice` calls; original accesses remain below. |
| INCHI-AUDIT-0028 | INCHI-F1684 `inchi_stricmp` | Performance: two checked byte accesses per character across repeated option comparisons | Extra per-character bounds/error paths confirmed statically; net elapsed cost unmeasured | Pinned C `third_party/InChI/INCHI-1-SRC/INCHI_BASE/src/util.c:1995..2018` reads `*s1` and `*s2` directly and advances native pointers. `INCHI_BASE/src/ichiparm.c` compares each option with many literals through this helper. | Rust `crates/cosmolkit-inchi/src/source/base/util.rs:4502..4565` resolves both heap slices once but calls `.get(index).ok_or(...)` for each string on every loop turn, plus checked index increment; `source/base/ichiparm.rs:439..656` shows repeated option comparisons. | For each compared character in option processing, Rust adds two bounds/error branches and checked index arithmetic beyond C direct pointer loads. Both are O(length), O(1) local space and allocation-free, but repeated comparisons make this a concrete possible parser hot-path regression. Valid NUL-terminated input results appear equivalent. | Option parser and API argument callers of `inchi_stricmp`. | Direct `panic!` inside the existing `loop` before the first `.get(index)` in `crates/cosmolkit-inchi/src/source/base/util.rs`; original reads remain below. |
| INCHI-AUDIT-0029 | INCHI-F1685 `inchi__strdup` | Behavior and performance: extra complete string allocation/copy before the source destination | Additional allocation and O(n) copy confirmed statically; elapsed impact unmeasured | Pinned C `third_party/InChI/INCHI-1-SRC/INCHI_BASE/src/util.c:2035..2049` obtains `strlen`, allocates `length+1` once with `inchi_malloc`, then `strcpy`s source directly into the destination. | Rust `crates/cosmolkit-inchi/src/source/base/util.rs:4567..4605` finds NUL then calls `source[..=length].to_vec()`, next calls `inchi_malloc` and `copy_from_slice`; the separate `inchi_malloc` zeroing regression is already INCHI-AUDIT-0012. | For every valid non-null n-byte string, Rust creates an extra n+1-byte Vec and copies the entire string into it before allocating the source-defined destination and copying again. Both are O(n), but Rust adds one allocation, one full copy and O(n) peak space. On allocation failure C returns a null pointer while Rust propagates `AllocationFailed` (or the temporary Vec allocation can fail before the source destination attempt); the direct panic at the first temporary clone conservatively covers this non-null input path too. | All `inchi__strdup` callers. | Direct `panic!` immediately before the `.to_vec()` expression in the non-null branch in `crates/cosmolkit-inchi/src/source/base/util.rs`; original clone and destination copy remain below. |
| INCHI-AUDIT-0030 | INCHI-F0091 `bHasChargedNeighbor` | Performance: checked neighbor/atom indexing in repeated restricted-proton atom scans | Extra per-neighbor branches confirmed statically; elapsed impact unmeasured | Pinned C `third_party/InChI/INCHI-1-SRC/INCHI_BASE/src/ichi_bns.c:12005..12017` directly reads `at[iat].neighbor[i]`, then `at[...].charge` for each neighbor; `:12117,12159,12237,12273,12306,12329` calls it repeatedly inside `AddRemoveProtonsRestr`. | Rust `crates/cosmolkit-inchi/src/source/base/ichi_bns.rs:85..125` obtains the center once but calls `.get` on the neighbor array and atom slice for every neighbor, with separate error propagation; `:487,534,690,733,771,800` invokes it in the corresponding atom scans. | For every valid neighbor in these atom loops, Rust adds two dynamic bounds/error paths beyond C direct indexing. Both are O(valence), O(1) space and allocation-free, but repeated checks in the per-atom restricted-proton path are a concrete possible hot-path regression. Valid graph results and state remain unchanged. | `AddRemoveProtonsRestr` and its reverse-structure callers. | Direct `panic!` at the beginning of the existing `for neighbor_index` loop in `crates/cosmolkit-inchi/src/source/base/ichi_bns.rs`; original checked reads remain below. |
| INCHI-AUDIT-0031 | INCHI-F0094 `allocate_zeroed` at six malloc-mapped callsites in pending ring-system function | Performance: full-buffer zero initialization absent from source `malloc` | Extra O(total allocated bytes) writes confirmed statically; elapsed impact unmeasured | Pinned C `third_party/InChI/INCHI-1-SRC/INCHI_BASE/src/ichi_bns.c:11479..11484` uses six `inchi_malloc` calls for `nStackAtom`, `nRingStack`, `nDfsNumber`, `nLowNumber`, conditional `nBondStack` and `cNeighNumb`; active `mode.h:1171..1173` maps `inchi_malloc` to plain `malloc`. The separate C `ichi_bns.c:1745` `pcValMinusBondsVal` use is `inchi_calloc` and legitimately zeroes. | Rust `crates/cosmolkit-inchi/src/source/base/ichi_bns.rs:1074..1083` always runs `values.resize(count, T::default())`; `:17429..17520` calls it at the six source `malloc` sites, while `:14728` calls it at the `calloc` site. | On every successful ring-system allocation of n elements, Rust writes n default values before the algorithm uses the buffers, whereas source malloc does not initialize them. Six buffers add O(total bytes) memory traffic and hot-path cost; no chemistry result difference is claimed. The separate shared `inchi_malloc` zeroing finding INCHI-AUDIT-0012 covers a different allocator helper. | Ring-system/cut-vertex computation and its callers; the `calloc` caller is excluded. | Six direct `panic!` markers immediately before each `allocate_zeroed` call associated with a C `inchi_malloc` line in `crates/cosmolkit-inchi/src/source/base/ichi_bns.rs`; the helper and calloc call remain intact. |
| INCHI-AUDIT-0032 | INCHI-F0095 `work_get` and INCHI-F0101 `bns_edge_index` | Performance: full arena/type/offset resolution on every BNS indexed read | Per-read operations confirmed statically; net elapsed cost unmeasured | Pinned C `third_party/InChI/INCHI-1-SRC/INCHI_BASE/src/ichi_bns.c:6045,6054,9750..9751,10815` reads `iedge[i]` and `Tree[u]` directly from allocated arrays in graph scans; no per-item allocation lookup occurs; `ichi_bns.c:6621,6625,7571` likewise reads `pVert->iedge[i]` directly. | Rust `crates/cosmolkit-inchi/src/source/base/ichi_bns.rs:1085..1095` calls `heap.slice(pointer.as_const())?`, then `.get(offset).cloned()` for every read. `source_types/mod.rs:982..999` shows allocation-ID lookup, slot type check, offset conversion and suffix bounds check inside `slice`; production callsites include BNS traversals at `ichi_bns.rs:7678..8351` and `:10154..10225`. | Each valid indexed read repeats arena lookup, type/provenance and slice validation beyond source direct indexing; repeated BNS edge/vertex traversals make this a concrete hot-path cost, though both remain O(1) per read. Cloning may add value-copy work; no particular heap-clone claim is made for current primitive callers. Shared downcast finding INCHI-AUDIT-0005 covers one component, while this finding covers repeated full pointer resolution. | All `work_get` and `bns_edge_index` callers in BNS graph and ring-system routines. | Direct `panic!` at each helper entry before `heap.slice` in `crates/cosmolkit-inchi/src/source/base/ichi_bns.rs`; no narrower branch distinguishes these read calls. Original access remains below. |
| INCHI-AUDIT-0033 | INCHI-F0096 `work_set`, INCHI-F0099 `bns_vertex_mut`, and INCHI-F0100 `bns_edge_mut` | Performance: full mutable arena/type/offset resolution on every BNS indexed write | Extra per-write work confirmed statically; net elapsed cost unmeasured | Pinned C `third_party/InChI/INCHI-1-SRC/INCHI_BASE/src/ichi_bns.c:9750..9751,10812..10815` directly assigns `Tree[u]`, `ScanQ[QSize]` and `BasePtr[Vertex_t]` in BNS initialization/traversal; `ichi_bns.c:2048,8868..8872` directly mutates BNS edge/vertex fields. | Rust `crates/cosmolkit-inchi/src/source/base/ichi_bns.rs:1098..1110` calls `heap.slice_mut(pointer)?`, `.get_mut(offset)` and error propagation for each write; `source_types/mod.rs:1002..1019` resolves arena slot, type/provenance and offset on every call. Rust `ichi_bns.rs:1136..1158` has analogous `bns_vertex_mut` and `bns_edge_mut` helpers, each resolving the arena and checking an index before returning a mutable record; production sites at `ichi_bns.rs:9936..10211` write through `work_set` repeatedly. | Each valid BNS array assignment pays a mutable arena lookup, type/provenance check and bounds/error path beyond C direct indexing. Both are O(1) per write and allocation-free, but repeated graph traversal updates make this a concrete hot-path cost. Shared downcast finding INCHI-AUDIT-0006 covers one component; this finding covers full repeated resolution. | Callers of all three mutable BNS access helpers. | Direct `panic!` at each of the three helper entries before `heap.slice_mut` in `crates/cosmolkit-inchi/src/source/base/ichi_bns.rs`; no narrower branch separates the repeated write path. Original write remains below. |
| INCHI-AUDIT-0034 | INCHI-F0097 `bns_vertex` and INCHI-F0098 `bns_edge` | Performance: whole BNS record clone and arena resolution for field reads | Full-value copy and lookup confirmed statically; elapsed impact unmeasured | Pinned C `third_party/InChI/INCHI-1-SRC/INCHI_BASE/src/ichi_bns.c:6618..6626` keeps `BNS_VERTEX *pVert1 = pBNS->vert + v1` and reads `pVert1->num_adj_edges`, `pVert1->iedge[i]` and neighboring vertex type directly. C `ichi_bns.c:6734..6745` similarly reads edge fields through `BNS_EDGE *pEdge`; `ichi_bns.h:189..223` defines both complete records. | Rust `crates/cosmolkit-inchi/src/source/base/ichi_bns.rs:1114..1124` calls `heap.slice(p_bns.vert.as_const())?`, `.get(offset).cloned()` and returns the complete `BNS_VERTEX`; `source/base/ichi_bns.rs:1126..1132` likewise resolves and clones complete `BNS_EDGE` records; `source_types/generated.rs:2400..2465` defines both records. Callers in `ichi_bns.rs:3649..3908` repeatedly request vertices and edges inside graph scans. | A field read through either helper resolves an arena slot/type/offset and copies the whole vertex or edge record instead of reading the needed field through an existing C pointer. This adds per-lookup work and full-record traffic in repeated BNS scans; asymptotic O(1) per lookup and no heap allocation remain. Any caller snapshot-state difference requires separate caller review. | All `bns_vertex` and `bns_edge` callers in BNS graph/ring routines. | Direct `panic!` at each helper entry before `heap.slice`, preserving both original copies below. |
| INCHI-AUDIT-0035 | INCHI-F0103 `input_atom_ref` INCHI-F0104 `input_atom_neighbor`, and INCHI-F0105 `input_atom_bond_type` | Performance: repeated checked atom/neighbor/bond access in BNS atom/neighbor scan | Extra per-access bounds/error path confirmed statically; net elapsed cost unmeasured | Pinned C `third_party/InChI/INCHI-1-SRC/INCHI_BASE/src/ichi_bns.c:2181..2230` advances `inp_ATOM *a` directly through atoms and reads `at[i]` or `a->neighbor[i1]` in `fix_special_bonds`; its branches revisit nearby atoms/neighbor fields; `ichi_bns.c:2235` directly reads `at[i].bond_type[i1]`. | Rust `crates/cosmolkit-inchi/src/source/base/ichi_bns.rs:1177..1180` converts every requested index and performs `atoms.get(offset).ok_or(...)`; `:1458..1805` calls it repeatedly in the corresponding `fix_special_bonds` atom and neighbor branches, often for individual fields; `ichi_bns.rs:1183..1191` separately checks every neighbor ordinal through `input_atom_neighbor`, and `ichi_bns.rs:1193..1201` checks each bond ordinal through `input_atom_bond_type`. | For valid atom indexes, Rust adds a checked/error branch and conversion on every atom-field revisit beyond C direct pointer/index access. Both are O(1) per access with no allocation, but the repeated checks in atom/neighbor scans are a concrete possible hot-path cost. No defined-input chemistry difference is claimed. | `fix_special_bonds` and other `input_atom_ref` callers. | Direct `panic!` at each helper entry before `.get`, retaining the original checked access. |
| INCHI-AUDIT-0036 | INCHI-F0113 `fix_special_bonds`, four-nonmetal-neighbor N(V) branch | Performance: repeated neighbor-list scan instead of reusing first ordinal | Two redundant helper calls confirmed statically; elapsed impact unmeasured | Pinned C `third_party/InChI/INCHI-1-SRC/INCHI_BASE/src/ichi_bns.c:2335..2348` assigns `i3 = nNoMetalOtherNeighIndex(at,n2,i)` once in the condition, reads `at[n2].bond_type[i3]`, then uses the same `i3` for the edge update. | Rust `crates/cosmolkit-inchi/src/source/base/ichi_bns.rs:1598..1614` calls `n_no_metal_other_neigh_index(Some(&atoms),n2,i)` once as a nonnegative predicate, again as the `input_atom_bond_type` ordinal, and a third time inside the success body. The callee `source/base/util.rs` scans the neighbor array linearly. | When the earlier N(V) predicates hold, Rust repeats the O(valence) neighbor search twice beyond C, including one repeat even if the bond-type condition fails. No defined-state result change is claimed; repeated search increases hot-path work in the BNS atom loop. | `fix_special_bonds` callers during BNS normalization. | Direct `panic!` immediately before the existing `if num_n == 1` condition in the four-neighbor branch of `source/base/ichi_bns.rs`; no existing boundary isolates only the second helper call without rewriting the expression, so this quarantines all candidates reaching that branch test. Original condition/body remain intact. |
| INCHI-AUDIT-0037 | INCHI-F0113 `fix_special_bonds`, four-nonmetal-neighbor N(V) loop | Performance: unconditional metal lookup before bond-type filter | Extra lookup and checked branches on single-bond neighbors confirmed statically; elapsed impact unmeasured | Pinned C `third_party/InChI/INCHI-1-SRC/INCHI_BASE/src/ichi_bns.c:2324..2329` tests `BOND_TYPE_DOUBLE` first, then dereferences the neighbor and calls `is_el_a_metal`; `INCHI_BASE/src/util.c:688..691` implements the metal lookup as one table access. | Rust `crates/cosmolkit-inchi/src/source/base/ichi_bns.rs:1584..1591` reads each neighbor and calls `is_el_a_metal` before testing `input_atom_bond_type(...) & 15 == 2`; Rust `source/base/util.rs:3060..3072` adds checked arithmetic, conversion, bounds branches and table access per call. | Every valid single-bond neighbor in this N(V) branch incurs a metal lookup and its error-path work that C short-circuits away. Same O(valence) loop and no extra allocation, but concrete avoidable per-neighbor work; no defined-state result difference claimed. | `fix_special_bonds` callers during BNS normalization. | Direct `panic!` immediately before the existing four-neighbor `for i1` loop in `source/base/ichi_bns.rs`, preserving the loop. This quarantines the loop for all candidates in that branch because no existing boundary isolates single-bond entries before the lookup. |
| INCHI-AUDIT-0038 | INCHI-F0113 `fix_special_bonds`, S(IV) neighbor classification | Performance: redundant linear neighbor-count call with identical outcomes | Extra scan on qualifying single bonds confirmed statically; elapsed impact unmeasured | Pinned C `third_party/InChI/INCHI-1-SRC/INCHI_BASE/src/ichi_bns.c:2399..2420` tests the OH/OM case, then sends every remaining bond directly to `num_other++` and `i4=i1` without another graph query. | Rust `crates/cosmolkit-inchi/src/source/base/ichi_bns.rs:1653..1671` evaluates `bond_type == 1 && 1 < n_no_metal_num_bonds(Some(&atoms), n1)?` between the OH/OM case and `else`, although both arms execute identical `num_other += 1; i4 = i1`; `source/base/util.rs` implements the count by a neighbor loop. | A valid single-bond neighbor that misses the OH/OM predicate receives an extra O(valence(n1)) scan in Rust even though its result cannot affect the action. No defined-state chemistry difference claimed; the full S(IV) atom loop can accrue these scans. | `fix_special_bonds` callers during BNS normalization. | Direct `panic!` inside the existing S(IV) neighbor loop immediately before `if bond_type == 2`; the expression has no narrower existing precondition boundary without restructuring the if/else chain, so this also quarantines double-bond classifications. Original logic remains below. |
| INCHI-AUDIT-0039 | INCHI-F0119 `mark_at_type`, totals initialization | Performance: checked scalar loop instead of contiguous bulk zero | Extra 33 checked index/error paths versus source bulk write confirmed statically; elapsed impact unmeasured | Pinned C `third_party/InChI/INCHI-1-SRC/INCHI_BASE/src/ichi_bns.c:5045..5048` executes one `memset(nAtTypeTotals, 0, ATTOT_ARRAY_LEN * sizeof(...))` when totals are present; C enum `ichi_bns.c:124` gives length 33. | Rust `crates/cosmolkit-inchi/src/source/base/ichi_bns.rs:3107..3113` iterates all 33 positions and calls `.get_mut(index).ok_or(...)` before each zero store; `ATTOT_ARRAY_LEN=33` at `:1279`. | On valid 33-entry totals, Rust performs 33 bounds/Option/error branches and individual stores instead of the C bulk-zero operation. Both O(33), no allocation, but a concrete fixed initialization overhead on every non-null totals call. No valid-state result difference claimed. | All `mark_at_type` callers that supply totals. | Direct `panic!` inside the existing `if let Some(totals)` branch immediately before the zeroing loop in `source/base/ichi_bns.rs`; original loop remains intact. |
| INCHI-AUDIT-0040 | INCHI-F0119 `mark_at_type`, per-atom type assignment | Performance: per-atom checked conversion and mutable lookup beyond direct source index | Extra per-atom index/error path confirmed statically; elapsed impact unmeasured | Pinned C `third_party/InChI/INCHI-1-SRC/INCHI_BASE/src/ichi_bns.c:5050..5054` assigns `atom[i].at_type = type` directly after `GetAtomChargeType` in the atom loop. | Rust `crates/cosmolkit-inchi/src/source/base/ichi_bns.rs:3115..3128` converts `i` through `usize::try_from`, calls `atoms.get_mut(offset).ok_or(...)`, then assigns `at_type`. | On every valid atom, Rust adds integer-conversion and bounds/error branches before the same field store. The O(num_atoms) loop and O(1) local space remain, but repeated checks are a concrete possible hot-path cost; no valid-state value difference claimed. | All `mark_at_type` callers. | Direct `panic!` in the existing atom loop immediately after the `GetAtomChargeType` result and before `let offset`, leaving the conversion and assignment intact. |
| INCHI-AUDIT-0041 | INCHI-F0120 `AddChangedAtHChargeBNS`, marked-atom scan and writes | Performance: repeated checked mark and atom accesses beyond direct source arrays | Extra per-atom and marked-atom bounds/error paths confirmed statically; elapsed impact unmeasured | Pinned C `third_party/InChI/INCHI-1-SRC/INCHI_BASE/src/ichi_bns.c:781..793` tests and clears `mark[i]` directly, calls `GetAtomChargeType`, and stores `at[i].at_type` directly. | Rust `crates/cosmolkit-inchi/src/source/base/ichi_bns.rs:3180..3197` converts every `i`, calls `mark.get(index).ok_or(...)` for the test, `mark.get_mut(index).ok_or(...)` again for marked items, then `atoms.get_mut(index).ok_or(...)` before type assignment. | Valid atom scans add checked index/error work on every mark probe and twice more on each marked item; both bodies remain O(num_atoms + classifications) and allocation-free, but the repeated access branches are a concrete possible hot-path cost. No valid-state result difference claimed. | All changed-atom recomputation callers. | Two direct `panic!` sites in the existing atom loop: before the `if *mark.get(...)` test and immediately before `atoms.get_mut(index)` after classification. The first quarantines mark test/clear work; the second documents assignment cost. Original code remains intact. |
| INCHI-AUDIT-0042 | INCHI-F0121 `AddOrRemoveExplOrImplH`, explicit-H array shift | Performance: per-record clone loop instead of source bulk `memmove` | Extra loop and full-record assignment per shifted atom confirmed statically; elapsed impact unmeasured | Pinned C `third_party/InChI/INCHI-1-SRC/INCHI_BASE/src/ichi_bns.c:962..967` saves one `inp_ATOM`, shifts `nNumRemovedExplicitH-i` contiguous records left with one `memmove`, then stores the removed record at the tail. | Rust `crates/cosmolkit-inchi/src/source/base/ichi_bns.rs:3406..3417` clones the removed record and iterates `current_index..shift_end`, cloning and assigning each `inp_ATOM` before saving the removed record; `source_types/generated.rs:1496..1532` shows `tagInputAtom` is a large `Copy`/`Clone` record with 20-entry arrays and stereo fields. | Each explicit-H removal with later saved records makes Rust execute a checked scalar loop and one full-atom clone/assignment per moved record instead of a bulk contiguous move. Both are O(records shifted), O(1) auxiliary storage and no heap allocation, but the per-record overhead is a concrete possible hot-path regression. The left-shift order preserves valid-state values. | Callers of explicit-H removal and stereo normalization. | Direct `panic!` inside the existing `if n_num_removed_explicit_h > i` branch immediately before `let removed = atoms[current_index].clone()`, preserving the original shift code. |
| INCHI-AUDIT-0043 | INCHI-F0122 `SubtractOrChangeAtHChargeBNS`, direct path/mark/atom accesses | Performance: repeated checked array resolution in alternating-path traversal | Extra per-pass and per-changed-atom bounds/error paths confirmed statically; elapsed impact unmeasured | Pinned C `third_party/InChI/INCHI-1-SRC/INCHI_BASE/src/ichi_bns.c:1070..1075,1123..1146` assigns `pBNS->alt_path = pBNS->altp[pass]`, directly tests/increments `mark[v1]`, and directly updates `at[v1].charge`. | Rust `crates/cosmolkit-inchi/src/source/base/ichi_bns.rs:3637..3643` converts `pass` and calls `pBNS.altp.get(...).ok_or(...)`; `:3690..3712` converts `v1` and uses `mark.get_mut(...).ok_or(...)` or `atoms.get_mut(...).ok_or(...)` on each changed atom. | Valid paths pay additional index conversions and bounds/error branches beyond C direct array operations. Both remain linear in path length, O(1) local space and allocation-free, but these are concrete possible traversal costs separate from already quarantined BNS arena helpers. No valid-state output difference claimed. | Alternating-path charge/H normalization callers. | Three direct `panic!` sites: before `pBNS.altp.get`, before `mark.get_mut` in the subtract branch, and before `atoms.get_mut` in the change branch; original reads/writes remain. |
| INCHI-AUDIT-0044 | INCHI-F0123 `EliminatePlusMinusChargeAmbiguity`, flow-correction branch | Performance: four full BNS record read/write cycles instead of field mutation | Full-record copies and mutable arena resolutions confirmed statically; elapsed impact unmeasured | Pinned C `third_party/InChI/INCHI-1-SRC/INCHI_BASE/src/ichi_bns.c:869..883` retains `edgePos`/`edgeNeg` pointers and adjusts only their `flow` plus `st_edge.cap/flow` on two group vertices. | Rust `crates/cosmolkit-inchi/src/source/base/ichi_bns.rs:3908..3927` obtains complete copied `BNS_EDGE` records, modifies their flow and writes each complete record via `bns_edge_mut`; it then obtains two complete copied `BNS_VERTEX` records, changes two fields each and writes both complete records via `bns_vertex_mut`. Generated BNS record layouts are in `source_types/generated.rs`; helper copy/arena costs are also recorded under INCHI-AUDIT-0033/0034. | For every ambiguity correction, Rust copies and writes back four whole records beyond C's selected field writes. The two edge and two vertex records are distinct in valid BNS group topology, so no defined-state result change is claimed. O(1) per correction and no allocation, but materially greater record traffic and repeated arena resolution. | Alternating-path charge ambiguity callers. | Direct `panic!` immediately inside the existing `if edge_pos.flow < edge_neg.flow` branch, before `dflow`/all writes; original branch remains intact. |
| INCHI-AUDIT-0045 | INCHI-F0124 `MarkAtomsAtTautGroups`, path-edge vertex classification | Performance: eager vertex lookups where source short-circuits | Two categories of avoidable full-vertex lookup confirmed statically; elapsed impact unmeasured | Pinned C `third_party/InChI/INCHI-1-SRC/INCHI_BASE/src/ichi_bns.c:1378..1381,1397..1402` evaluates `pBNS->vert[v2].type` only after `v1` is a T-group/temp vertex in the first `&&` condition, and evaluates `pBNS->vert[v1].type` only after `v2` is a T-group/temp vertex in the second condition. | Rust `crates/cosmolkit-inchi/src/source/base/ichi_bns.rs:4215..4247` computes `v2_atom` via `bns_vertex(heap,pBNS,v2)` independently before testing `v1_group_or_temp`; in the `else` arm it computes `v1_atom` via another `bns_vertex` independently before testing `v2_group_or_temp`. `bns_vertex` resolves an arena pointer and copies a full vertex (INCHI-AUDIT-0034). | On valid path edges where the earlier group test is false, Rust performs a full vertex resolution and copy that C avoids. Both remain O(path edges) and allocation-free, but these are concrete per-edge hot-path costs. No valid-state category result difference claimed. | Tautomer-group path marking callers. | Direct `panic!` immediately before each existing `let v2_atom` and `let v1_atom` computation in `source/base/ichi_bns.rs`; original predicates remain unchanged. |
| INCHI-AUDIT-0046 | INCHI-F0124 `MarkAtomsAtTautGroups`, fixed scratch arrays | Performance: eager zero initialization absent from source stack arrays | Two full 127-entry zero writes confirmed statically; elapsed impact unmeasured | Pinned C `third_party/InChI/INCHI-1-SRC/INCHI_BASE/src/ichi_bns.c:1339..1341` declares uninitialized `S_CHAR cDelta[MAX_ALT_AATG_ARRAY_LEN]` and `AT_NUMB nVertex[MAX_ALT_AATG_ARRAY_LEN]`; source writes each position before use at `:1390..1439`, with active max 127. | Rust `crates/cosmolkit-inchi/src/source/base/ichi_bns.rs:4166..4169` initializes both 127-element arrays with zeros before scanning paths, then writes selected positions before reads at `:4227..4269`. | Rust performs full-array initialization on every call, including no-path calls, beyond C stack reservation and source-defined writes. This is fixed O(127) memory traffic, no extra heap allocation or valid-state result change, but a concrete avoidable call cost. | Tautomer-group path marking callers. | Direct `panic!` immediately before the two existing scratch-array initializers in `source/base/ichi_bns.rs`; original declarations remain below. |
| INCHI-AUDIT-0047 | INCHI-F0126 `SimpleRemoveAcidicProtons`, selected-atom updates | Performance: repeated heap pointer resolution around totals and tautomer info | Three full arena resolutions per removed atom confirmed statically; elapsed impact unmeasured | Pinned C `third_party/InChI/INCHI-1-SRC/INCHI_BASE/src/ichi_bns.c:4177..4183` passes `pAATG->nAtTypeTotals` directly to two `GetAtomChargeType` calls and `pAATG->t_group_info` directly to one `AddOrRemoveExplOrImplH` call. | Rust `crates/cosmolkit-inchi/src/source/base/ichi_bns.rs:4633..4664` calls `heap.slice_mut(p_aatg.nAtTypeTotals)?` before each classification and `heap.slice_mut(p_aatg.t_group_info)?.get_mut(0)` before H removal. `source_types/mod.rs:1002..1019` shows allocation-slot/type/offset resolution in each `slice_mut`. | For each selected valid acidic atom, Rust repeats three arena lookups, type/provenance and bounds/error paths before the same callee work; C passes already available direct pointers. Both remain O(selected atoms × callee cost), allocation-free, but repeated resolution is a concrete possible hot-path cost. Shared downcast cost INCHI-AUDIT-0006 is one component; this finding covers all three callsite resolutions. No valid-state result difference claimed. | Acidic-proton removal callers. | Three direct `panic!` sites immediately before the first totals `heap.slice_mut`, the tautomer-info `heap.slice_mut`, and the second totals `heap.slice_mut` in the existing selected-atom branch; original calls remain. |
| INCHI-AUDIT-0048 | INCHI-F0129 `SimpleAddAcidicProtons`, selected-atom updates | Performance: repeated heap pointer resolution around totals and tautomer info | Three full arena resolutions per protonated atom confirmed statically; elapsed impact unmeasured | Pinned C `third_party/InChI/INCHI-1-SRC/INCHI_BASE/src/ichi_bns.c:4301..4307` passes `pAATG->nAtTypeTotals` directly to two `GetAtomChargeType` calls and `pAATG->t_group_info` directly to one `AddOrRemoveExplOrImplH` call. | Rust `crates/cosmolkit-inchi/src/source/base/ichi_bns.rs:4915..4945` calls `heap.slice_mut(p_aatg.nAtTypeTotals)?` before both classifications and `heap.slice_mut(p_aatg.t_group_info)?.get_mut(0)` before adding H. `source_types/mod.rs:1002..1019` resolves the allocation, type/provenance and offset each time. | For each selected valid atom, Rust repeats three arena lookups and error/bounds paths beyond C direct pointer passing. Same O(selected atoms × callee cost), O(1) local state and no body heap allocation; concrete possible per-atom hot-path cost. Symmetric removal-path cost is INCHI-AUDIT-0047. No valid-state result difference claimed. | Acidic-proton addition callers. | Three direct `panic!` sites immediately before first totals resolution, tautomer-info resolution and second totals resolution in the existing selected-atom branch; original calls remain. |
| INCHI-AUDIT-0049 | INCHI-F0135 `HardRemoveAcidicProtons` and nested INCHI-F0136/INCHI-F0137 atom helpers | Performance: repeated whole atom-array allocation, clone, and writeback around BNS group operations | Extra full-array traffic and allocations confirmed statically; elapsed impact unmeasured | Pinned C `third_party/InChI/INCHI-1-SRC/INCHI_BASE/src/ichi_bns.c:4385..4411` passes the existing `at` pointer to two `CreateCGroupInBnStruct` and two `CreateTGroupInBnStruct` calls; `:4480..4517` passes the same pointer to up to four `RemoveLastGroupFromBnStruct` calls, with no caller-side atom-array allocation or whole-array copy. | Rust `crates/cosmolkit-inchi/src/source/base/ichi_bns.rs:5309..5338` defines `clone_atoms` using `atoms[..count].to_vec()` and `write_atoms` using `destination[..count].clone_from_slice(...)`; `:5362..5403` clones the entire `num_atoms` prefix before each of four group constructors, and `:5482..5491` clones the prefix then writes it back for each active removal. `inp_ATOM` is a large record declared in `source_types/generated.rs:1496..1532`. | Every invocation makes four O(num_atoms) heap allocations and full-record copies before BNS group creation; up to four removals add four allocations/copies and four full-record writebacks. C retains the existing pointer and mutates through it in place. Rust adds O(num_atoms) peak temporary space and O(num_atoms) copy work per operation, up to eight temporary arrays, beyond source work; defined-state value correspondence remains under review. | `HardRemoveAcidicProtons` callers and the two nested helper call paths. | Direct `panic!` immediately before `Ok(atoms[..count].to_vec())` in `clone_atoms` and immediately before `destination[..count].clone_from_slice(...)` in `write_atoms`; original expressions remain below. |
| INCHI-AUDIT-0050 | INCHI-F0135 `HardRemoveAcidicProtons` and nested INCHI-F0138 `aatg_total` | Performance: arena resolution and checked lookup for every scalar totals read | Repeated pointer resolution and bounds/error paths confirmed statically; elapsed impact unmeasured | Pinned C `third_party/InChI/INCHI-1-SRC/INCHI_BASE/src/ichi_bns.c:4375..4376,4416..4470,4523..4543` reads `pAATG->nAtTypeTotals[index]` directly for initial counts, each alternating-path iteration, conditional charge tests, and final checks; the pointer is held in the same `BN_AATG` throughout. | Rust `crates/cosmolkit-inchi/src/source/base/ichi_bns.rs:5342..5349` implements `aatg_total` by `heap.slice(p_aatg.nAtTypeTotals.as_const())?`, then `.get(index).copied().ok_or(...)`; `:5351..5359,5410..5471,5505..5520` calls this separately for every scalar read. `source_types/mod.rs:980..1019` shows arena slot/type/offset resolution. | Each valid scalar totals read repeats a full virtual-pointer resolution and checked/error path where C directly indexes the already-held pointer. The loops still follow source iteration counts and no allocation occurs here, but every path iteration pays multiple extra lookups; no defined-state value difference is claimed. | `HardRemoveAcidicProtons` callers and its nested `aatg_total` helper. | Direct `panic!` at the entry of `aatg_total`, immediately before `heap.slice`; the original read remains below. |
| INCHI-AUDIT-0051 | INCHI-F0139 `HardAddAcidicProtons` and nested INCHI-F0140/F0141 atom helpers | Performance: repeated whole atom-array allocation, clone, and writeback around BNS groups | Extra full-array allocations and traffic confirmed statically; elapsed impact unmeasured | Pinned C `third_party/InChI/INCHI-1-SRC/INCHI_BASE/src/ichi_bns.c:4590..4614` passes the existing `at` to three C-group and one T-group constructor; `:4695..4734` removes up to four groups using that same pointer, with no caller-side array allocation or copy. Source constructors `:3599..3987` read atoms and mutate BNS, while remover `:3988..4089` writes first-atom flags in place. | Rust `crates/cosmolkit-inchi/src/source/base/ichi_bns.rs:5577..5606` defines `clone_atoms` with `atoms[..count].to_vec()` and `write_atoms` with `destination[..count].clone_from_slice(...)`; `:5633..5676` clones before each of four constructors and `:5757..5767` clones then writes back for each active group removal. `inp_ATOM` is the large `Copy` record at `source_types/generated.rs:1496..1532`. | On valid state, Rust makes four O(num_atoms) allocations and full-record copies before group creation, then up to four more allocations/copies and four full-array writebacks for removal; C uses existing atom storage. This adds O(num_atoms) peak temporary storage and repeated memory traffic beyond source work. Cloned records still carry the same values; behavioral correspondence is reviewed separately. | `HardAddAcidicProtons` callers and its nested clone/writeback helpers. | Direct `panic!` before the `to_vec()` in this function's nested `clone_atoms` and before `clone_from_slice` in its nested `write_atoms`; originals remain. |
| INCHI-AUDIT-0052 | INCHI-F0139 `HardAddAcidicProtons` and nested INCHI-F0142 `aatg_total` | Performance: repeated arena resolution and checked lookup per scalar totals read | Extra per-read lookup/error paths confirmed statically; elapsed impact unmeasured | Pinned C `third_party/InChI/INCHI-1-SRC/INCHI_BASE/src/ichi_bns.c:4583..4584,4624..4692,4742..4754` directly indexes `pAATG->nAtTypeTotals` for initial charges, alternating-path loop counts, conditional tests and final checks. | Rust `crates/cosmolkit-inchi/src/source/base/ichi_bns.rs:5610..5620` resolves `heap.slice(p_aatg.nAtTypeTotals.as_const())?` and performs checked `.get(index)` on each `aatg_total` call; `:5622..5627,5682..5753,5777..5796` calls it for each scalar. Arena lookup costs appear in `source_types/mod.rs:980..1019`. | Every valid totals read repeats slot/type/offset resolution and checked error handling beyond C direct pointer indexing. Loop order and O(1) per-read asymptotics remain; no heap allocation or defined-state result difference is claimed, but repeated work is a concrete possible hot-path cost. | `HardAddAcidicProtons` callers and nested totals helper. | Direct `panic!` immediately before `heap.slice` in this function's nested `aatg_total`; original expression remains. |
| INCHI-AUDIT-0053 | INCHI-F0143 `HardRemoveHplusNP` with nested INCHI-F0144/F0145 atom helpers | Performance: full atom-array clone/allocation and writeback around group operations | Added O(num_atoms) allocations/copies confirmed statically; elapsed impact unmeasured | Pinned C `third_party/InChI/INCHI-1-SRC/INCHI_BASE/src/ichi_bns.c:4814..4830` gives the existing `at` pointer to two charge-group and one H-group constructor; `:4962..4995` removes up to three active groups through that same pointer, without caller-side full-array copies. Source constructors `:3599..3987` only read atoms, and remover `:3988..4089` updates first-atom flags in place. | Rust `crates/cosmolkit-inchi/src/source/base/ichi_bns.rs:5904..5933` defines `clone_atoms` with `atoms[..count].to_vec()` and `write_atoms` with `destination[..count].clone_from_slice(...)`; `:5966..5994` clones before each constructor and `:6085..6095` clones and writes back for each active removal. `inp_ATOM` is a large `Copy` record at `source_types/generated.rs:1496..1532`. | Every invocation makes three O(num_atoms) heap allocations/full-record copies before BNS group creation, then up to three more allocations/copies and full-array writebacks. C retains the original array. Rust adds O(num_atoms) peak temporary storage and repeated memory traffic; no valid-state value difference from cloning is claimed. | `HardRemoveHplusNP` callers and its two nested atom helpers. | Direct `panic!` immediately before `to_vec()` in nested `clone_atoms` and before `clone_from_slice` in nested `write_atoms`; original code stays below. |
| INCHI-AUDIT-0054 | INCHI-F0143 `HardRemoveHplusNP` and nested INCHI-F0146 `aatg_total` | Performance: repeated virtual totals resolution versus direct pointer-indexed reads | Repeated arena slot/type/offset and bounds/error work confirmed statically; elapsed impact unmeasured | Pinned C `third_party/InChI/INCHI-1-SRC/INCHI_BASE/src/ichi_bns.c:4799..4800,4881..4954,5006..5031` reads `pAATG->nAtTypeTotals[index]` directly for initial counts, path-loop charge checks, neutralization and final charge invariant. | Rust `crates/cosmolkit-inchi/src/source/base/ichi_bns.rs:5935..5944` defines `aatg_total` with `heap.slice(p_aatg.nAtTypeTotals.as_const())?` followed by checked `.get(index)`; `:5955..5959,6000..6082,6105..6124` invokes it for each scalar read. The virtual heap lookup is defined in `source_types/mod.rs:980..1019`. | Each valid scalar read incurs an arena resolution, type/offset and bounds/error path where C indexes the retained totals pointer. Both remain O(1) per read and allocation-free, but repeated loop reads add concrete hot-path work. No valid-state value difference claimed. | `HardRemoveHplusNP` callers and nested totals helper. | Direct `panic!` immediately before `heap.slice` in its nested `aatg_total`, retaining the original read. |
| INCHI-AUDIT-0055 | INCHI-F0143 `HardRemoveHplusNP` and nested INCHI-F0147 `removed_protons` | Performance: repeated arena lookup for source direct tautomer counter | Two virtual-pointer resolutions per path iteration confirmed statically; elapsed impact unmeasured | Under active `FIX_REM_PROTON_COUNT_BUG=1` (`third_party/InChI/INCHI-1-SRC/INCHI_BASE/src/mode.h:310`), pinned C `ichi_bns.c:4883..4895,4926..4937` reads `pAATG->t_group_info->tni.nNumRemovedProtons` directly before and after each `bExistsAltPath` call in removal and neutralization loops. | Rust `crates/cosmolkit-inchi/src/source/base/ichi_bns.rs:5946..5953` defines `removed_protons` by resolving `heap.slice(p_aatg.t_group_info.as_const())?`, checked `.first()`, then field read; `:6003,6020,6047,6064` invokes it twice per loop iteration. `source_types/mod.rs:980..1019` defines the arena lookup. | For valid group info, each loop iteration gains two virtual-pointer slot/type/offset resolutions and checked/error branches beyond C direct field reads. The loop remains source-equivalent and allocation-free, but this is concrete repeated hot-path work. No value difference is claimed for source-sized counters. | `HardRemoveHplusNP` callers and nested `removed_protons`. | Direct `panic!` at the nested helper entry before `heap.slice`; the original field read remains. |
| INCHI-AUDIT-0056 | INCHI-F0148 `RemoveNPProtonsAndAcidCharges`, simple N/P/O proton count mismatch | Behavior/state: normalization flag set after source would jump to error exit | Concrete control-flow divergence for positive short removal; reachable input not dynamically established | Pinned C `third_party/InChI/INCHI-1-SRC/INCHI_BASE/src/ichi_bns.c:5120..5129` calls `SimpleRemoveHplusNPO`, and if `ret != num` sets `BNS_PROGRAM_ERR` then immediately `goto exit_function`, skipping `FLAG_PROTON_NPO_SIMPLE_REMOVED`. | Rust `crates/cosmolkit-inchi/src/source/base/ichi_bns.rs:6274..6289` sets `b_error` when `ret != num`, but continues into `if ret > 0` and sets `FLAG_PROTON_NPO_SIMPLE_REMOVED` before storing group info. | If the source helper removes a positive number fewer than `num`, both return a program error, but Rust also leaves the simple-removal normalization flag set while C does not. Atom/totals mutations from the callee are already present on both sides; this finding concerns the additional flag mutation. | Callers of `RemoveNPProtonsAndAcidCharges` observing normalization flags after error. | Direct `panic!` inside the existing `if ret != num` branch before `b_error = BNS_PROGRAM_ERR`, preserving all original code. |
| INCHI-AUDIT-0057 | INCHI-F0148 `RemoveNPProtonsAndAcidCharges`, final charge-cancellation flag on error | Behavior/state: fallthrough executes flag update that source error jump skips | Concrete control-flow divergence on a prior-cancellation/later-error path; reachability unmeasured | Pinned C `third_party/InChI/INCHI-1-SRC/INCHI_BASE/src/ichi_bns.c:5123..5221` jumps to `exit_function` on each error, bypassing the `FLAG_PROTON_CHARGE_CANCEL` OR at `:5227`; `:5229..5235` returns the error. | Rust `crates/cosmolkit-inchi/src/source/base/ichi_bns.rs:6283..6399` accumulates `b_error` and guards later chemistry, but always executes the final `tgroup_info` load and conditional `FLAG_PROTON_CHARGE_CANCEL` OR at `:6401..6405` before mapping `b_error` at `:6407..6415`. | If an earlier hard path sets a nonzero canceled-charge count and a later path errors, Rust sets the cancellation normalization flag before returning the error; C's error jump skips that flag. Even when the count is zero, Rust performs a full group-info load/store on source error exits. | Callers inspecting normalization flags or error-path state from `RemoveNPProtonsAndAcidCharges`. | Direct conditional `panic!` immediately before Rust's final unconditional `tgroup_info` load when `b_error != 0`, preserving the original load, flag update, and return below. |
| INCHI-AUDIT-0058 | INCHI-F0148 `RemoveNPProtonsAndAcidCharges` and nested INCHI-F0149/F0150 atom helpers | Performance: whole live-plus-explicit-H atom clone and writeback around simple proton operations | Extra array allocations/full-record traffic confirmed statically; elapsed impact unmeasured | Pinned C `third_party/InChI/INCHI-1-SRC/INCHI_BASE/src/ichi_bns.c:5120..5129,5151..5180,5191..5220` passes the existing `at` pointer directly to simple proton helpers, which mutate that storage; the caller makes no full atom-array allocation or writeback. | Rust `crates/cosmolkit-inchi/src/source/base/ichi_bns.rs:6178..6212` defines `clone_atoms` that resolves `nNumRemovedExplicitH` then allocates/copies `num_atoms + removed_explicit_h` full `inp_ATOM` records via `to_vec()`, and `write_atoms` copies that prefix back with `clone_from_slice`. Rust calls the pair at `:6275..6289,6315..6326,6344..6354,6357..6368,6386..6396`; `inp_ATOM` layout is in `source_types/generated.rs:1496..1532`. | Every entered simple branch adds an O(num_atoms + explicit H) allocation and full-record copy, then full-prefix writeback; C mutates the existing array. Up to five such pairs can occur across branch paths. Rust adds O(num_atoms + explicit H) peak temporary storage and repeated memory traffic. Value correspondence is reviewed separately. | `RemoveNPProtonsAndAcidCharges` callers and its nested atom helpers. | Direct `panic!` immediately before `to_vec()` in nested `clone_atoms` and before `clone_from_slice` in nested `write_atoms`; original expressions remain. |
| INCHI-AUDIT-0059 | INCHI-F0148 `RemoveNPProtonsAndAcidCharges` and nested INCHI-F0151/F0152 group-info helpers | Performance: repeated full group-info clone/writeback for direct flag reads and writes | Whole-struct traffic and repeated arena resolutions confirmed statically; elapsed impact unmeasured | Pinned C `third_party/InChI/INCHI-1-SRC/INCHI_BASE/src/ichi_bns.c:5096..5102,5129,5143,5159,5180,5202,5220,5227` retains one `T_GROUP_INFO *`, reads four tautomer flags directly and updates one `tni.bNormalizationFlags` field per successful branch; `ichitaut.h:222..240` defines the full record. | Rust `crates/cosmolkit-inchi/src/source/base/ichi_bns.rs:6214..6236` resolves the virtual group-info pointer and clones the full `T_GROUP_INFO` on each `tgroup_info` call, then resolves a mutable pointer and writes the whole record through `store_tgroup_info`; `:6250..6254,6277..6289,6308..6312,6323..6325,6351..6353,6365..6367,6393..6395,6401..6405` uses these helpers. Rust record fields are at `source_types/generated.rs:2728..2743`. | Initial flag-gate setup copies the whole group-info record where C reads four bits. Each normalization-flag update adds full-record read, clone and writeback plus two arena resolutions where C ORs one field through a retained pointer. No heap allocation from the shallow record clone is evident, but repeated record traffic is a concrete possible hot-path cost. | `RemoveNPProtonsAndAcidCharges` callers and two nested group-info helpers. | Direct `panic!` before the `.clone()` return in `tgroup_info` and before `*target = value` in `store_tgroup_info`; originals remain. |
| INCHI-AUDIT-0060 | INCHI-F0148 `RemoveNPProtonsAndAcidCharges` and nested INCHI-F0153 `total` | Performance: repeated virtual-pointer resolution for direct totals reads | Extra per-read arena/bounds work confirmed statically; elapsed impact unmeasured | Pinned C `third_party/InChI/INCHI-1-SRC/INCHI_BASE/src/ichi_bns.c:5120,5132,5146..5165,5189..5205` directly indexes `pAATG->nAtTypeTotals` for the proton and charge branch decisions and passes that pointer to callees. | Rust `crates/cosmolkit-inchi/src/source/base/ichi_bns.rs:6238..6243` implements each `total` call with `heap.slice(p_aatg.nAtTypeTotals.as_const())?` and checked `.get(index)`; `:6274,6292,6315..6316,6328..6329,6357..6358,6370..6371` repeats the helper for scalar decisions, often twice for the same index. `source_types/mod.rs:980..1019` resolves the arena slot/type/offset. | Every valid totals read pays a virtual arena lookup and bounds/error branch beyond C direct pointer indexing; some branches load the same scalar twice before any intervening mutation. Both use O(1) per read and no allocation, but this is concrete avoidable branch overhead. No defined-state value difference claimed. | `RemoveNPProtonsAndAcidCharges` callers and nested totals helper. | Direct `panic!` at the helper entry before `heap.slice`, preserving the original read. |
| INCHI-AUDIT-0061 | INCHI-F0154 `CreateCGroupInBnStruct`, selected c-point construction | Performance: full BNS record snapshot/writeback cycles instead of retained vertex/edge pointers | Additional whole-record traffic and arena resolutions per selected point confirmed statically; elapsed impact unmeasured | Pinned C `third_party/InChI/INCHI-1-SRC/INCHI_BASE/src/ichi_bns.c:3690..3800` retains `BNS_VERTEX *vert_ficpoint`, `*vertex_cpoint`, and `BNS_EDGE *edge` pointers, mutates their type, flow/cap, adjacency and edge fields in place, and reads adjacent edge/vertex fields through pointers in the active `FIX_CPOINT_BOND_CAP=1` loop. | Rust `crates/cosmolkit-inchi/src/source/base/ichi_bns.rs:6697..6785` clones two full `BNS_VERTEX` records and one full `BNS_EDGE` record for every selected c-point, clones each adjacent edge and possibly center vertex, then writes the complete selected edge and two vertices back through mutable arena helpers; `source_types/generated.rs:2400..2465` defines those records. Shared helper-resolution/copy costs are INCHI-AUDIT-0033/0034. | Each selected c-point adds at least three full-record reads and three full-record writebacks, plus per-adjacent-edge record copies and virtual-pointer resolutions where C keeps direct pointers. The scan remains O(num_atoms + selected adjacency), O(1) local records and allocation-free in this body, but record traffic and hot-path lookup costs are materially greater. Snapshot-state equivalence is reviewed separately. | BNS hard proton/charge group constructors and their callers. | Direct `panic!` immediately after the selected-point classification `continue` gate and before `let fictpoint` in Rust's second c-point loop; original construction remains below. |
| INCHI-AUDIT-0062 | INCHI-F0155 `CreateTGroupInBnStruct`, selected endpoint construction | Performance: full BNS record snapshots and writebacks instead of retained pointers | Per-endpoint whole-record traffic and virtual arena resolutions confirmed statically; elapsed impact unmeasured | Pinned C `third_party/InChI/INCHI-1-SRC/INCHI_BASE/src/ichi_bns.c:3890..3971` holds `vert_ficpoint`, `vert_endpoint`, and `edge` as pointers and directly updates endpoint type, new edge flow/cap, adjacent edge capacities, adjacency lists and BNS counters; source loop uses direct adjacent edge and center vertex reads. | Rust `crates/cosmolkit-inchi/src/source/base/ichi_bns.rs:7023..7118` clones two complete `BNS_VERTEX` values and one complete `BNS_EDGE` for each selected endpoint, clones each adjacent edge and sometimes a center vertex, then writes all three full records back through mutable arena helpers. Record definitions are `source_types/generated.rs:2400..2465`; helper costs are INCHI-AUDIT-0033/0034. | Every selected endpoint adds at least three whole-record snapshots and three whole-record writes, plus adjacent-edge/center-vertex copies and arena lookups, where C retains direct pointers. Both remain O(num_atoms + selected adjacency) and allocation-free in this body, but Rust has materially greater record traffic. Local snapshot state is reviewed for equivalence separately. | Hard proton transfer and BNS tautomer-group callers. | Direct `panic!` immediately after the selected-endpoint classification gate and before `let fictpoint` in Rust's second endpoint loop; existing construction remains. |
| INCHI-AUDIT-0063 | INCHI-F0156 `bAddNewVertex`, successful vertex/edge insertion | Performance: repeated whole BNS record copies and arena writes versus source pointers | Multiple full-record snapshots/writebacks confirmed statically; elapsed impact unmeasured | Pinned C `third_party/InChI/INCHI-1-SRC/INCHI_BASE/src/ichi_bns.c:6491..6552` obtains direct pointers to the old, new and last vertices and new edge, then mutates their fields and adjacency entries in place after three capacity guards. | Rust `crates/cosmolkit-inchi/src/source/base/ichi_bns.rs:7218..7294` clones the last vertex, old vertex, new edge and new vertex, rereads/clones the old vertex after edge write, then writes the entire edge and both vertices via mutable arena helpers; adjacency writes separately use `work_set`. The shared helper overhead is INCHI-AUDIT-0033/0034; BNS record definitions are `source_types/generated.rs:2400..2465`. | Each successful insertion adds several full-record copies and three full-record writebacks plus repeated virtual-pointer resolution where C holds direct pointers. The operation stays O(1) local state and allocation-free, but incurs materially greater fixed hot-path memory traffic. Defined-state correspondence is reviewed separately. | BNS temporary-vertex insertion callers. | Direct `panic!` after all existing capacity guards, immediately before `let mut edge = bns_edge(...)`, preserving the original construction below. |
| INCHI-AUDIT-0064 | INCHI-F0157 `AddNewEdge`, successful edge insertion | Performance: whole endpoint vertex snapshots and writebacks beyond direct source pointers | Full-record copies, arena resolutions and separately checked adjacency writes confirmed statically; elapsed impact unmeasured | Pinned C `third_party/InChI/INCHI-1-SRC/INCHI_BASE/src/ichi_bns.c:6556..6606` receives two `BNS_VERTEX *` values, checks their fields, zeroes one edge, then mutates both endpoint records and adjacency slots directly; even aliased endpoints use those same pointers. | Rust `crates/cosmolkit-inchi/src/source/base/ichi_bns.rs:7365..7444` clones two complete endpoint vertices through `bns_vertex`, checks virtual pointer differences, uses `work_set` for adjacency, then writes one or two complete vertices plus a complete edge via mutable arena helpers. Shared helper costs are INCHI-AUDIT-0033/0034. | Every successful distinct-endpoint insertion adds two whole-vertex copies, two full-vertex writebacks and multiple arena resolutions beyond source retained pointers; self-endpoint insertion still clones both aliases and writes one complete record. Both remain O(1) and allocation-free here, but the repeated record traffic is a concrete hot-path cost. Defined-state endpoint ordering is reviewed separately. | BNS graph edge-construction callers. | Direct `panic!` after all existing bounds/capacity guards and before `let mut edge = BNS_EDGE::default()`; original edge construction remains. |
| INCHI-AUDIT-0065 | INCHI-F0160 `bAddStCapToAVertex`, cap adjustment | Performance: full BNS record copies and checked old-cap writes versus direct pointers | Extra whole-record traffic and per-adjacent-edge bounds/error paths confirmed statically; elapsed impact unmeasured | Pinned C `third_party/InChI/INCHI-1-SRC/INCHI_BASE/src/ichi_bns.c:6717..6768` keeps `pVert1` and each `pEdge` as pointers, directly stores old caps in `nOldCapVertSingleBond[n++]`, increments the vertex cap, then reads neighbor vertices and updates each eligible edge cap in place. | Rust `crates/cosmolkit-inchi/src/source/base/ichi_bns.rs:7715..7759` clones the full source vertex, performs checked `nOldCapVertSingleBond.get_mut(...).ok_or(...)` for the first and every adjacent edge cap, writes the whole vertex, clones each adjacent edge and neighbor vertex, then writes the whole modified edge. Shared arena/record helper costs are INCHI-AUDIT-0033/0034. | Every valid call adds a whole-vertex read/write and checked old-cap write; each adjacency adds checked array access and full edge/neighbor-vertex reads, with full edge writeback on eligible edges. Both are O(adjacency) and allocation-free, but Rust has materially more record traffic and per-edge branches than C. No defined-state result difference claimed. | Alternating-path setup callers of `bAddStCapToAVertex`. | Direct `panic!` before the initial `let mut vert1 = bns_vertex(...)`, preserving all existing cap-update logic. |
| INCHI-AUDIT-0066 | INCHI-F0161 `bSetBnsToCheckAltPath` and nested INCHI-F0163 `set_fcd_sentinel` | Performance: complete flow-change record read/write for one sentinel field | Whole-record traffic and two arena resolutions per sentinel confirmed statically; elapsed impact unmeasured | Pinned C `third_party/InChI/INCHI-1-SRC/INCHI_BASE/src/ichi_bns.c:6850,6994,7180,7205` directly sets `fcd[ifcd].iedge = NO_VERTEX` at active branch setup; `ichi_bns.h:332..342` defines a multi-field `BNS_FLOW_CHANGES` record. | Rust `crates/cosmolkit-inchi/src/source/base/ichi_bns.rs:7813..7821` calls `work_get` to copy the entire record, changes only `iedge`, then calls `work_set` to write the entire record; `:7855,7971,8117,8133` invokes the helper in corresponding setup paths. Shared arena work helper cost is INCHI-AUDIT-0033. | Each valid sentinel update performs a full-record read and full-record write plus two virtual-pointer resolutions instead of one C field store. Both O(1) and allocation-free, but this is concrete repeated setup traffic. Other fields are preserved by the copy, so no valid-state value difference is claimed. | Alternating-path setup callers and nested sentinel helper. | Direct `panic!` immediately before `let mut entry = work_get(...)` in `set_fcd_sentinel`, preserving the original read/write below. |
| INCHI-AUDIT-0067 | INCHI-F0161 `bSetBnsToCheckAltPath` and nested INCHI-F0164 `advance_fcd` | Performance: whole flow-change record clone for sentinel-only scan | Per-entry record copy and arena resolution confirmed statically; elapsed impact unmeasured | Pinned C `third_party/InChI/INCHI-1-SRC/INCHI_BASE/src/ichi_bns.c:7225..7228,7245..7248,7265..7268,7286..7289` advances `ifcd` by directly testing only `fcd[ifcd].iedge` after each active `bSetFlowToCheckOneBond` call. The nine-field record is declared at `ichi_bns.h:332..342`. | Rust `crates/cosmolkit-inchi/src/source/base/ichi_bns.rs:7824..7832` calls `work_get::<BNS_FLOW_CHANGES>` for each scanned entry, which resolves a virtual slice and clones the full record (`:1086..1097`), then reads only `iedge`; `:8144,8154,8164,8174` invokes it after the four corresponding bond-flow calls. | Each valid scan entry copies all nine fields and performs virtual arena/type/bounds work instead of a single C field load. Both scans remain O(number of flow-change entries), O(1) local space and allocation-free, but repeated full-record traffic is a concrete possible hot-path regression. Shared read-resolution cost is INCHI-AUDIT-0032. No valid-state value difference is claimed. | Endpoint neutralization paths and callers of `bSetBnsToCheckAltPath`. | Direct `panic!` at the start of nested `advance_fcd`, before its `while work_get` loop; the original scan remains below. |
| INCHI-AUDIT-0068 | INCHI-F0165 `bRestoreBnsAfterCheckAltPath`, active temporary-vertex removal and old-cap restore branches | Performance: repeated full BNS vertex/edge snapshots and writebacks for source field mutations | Per-edge and per-cap full-record traffic confirmed statically; elapsed impact unmeasured | Pinned C `third_party/InChI/INCHI-1-SRC/INCHI_BASE/src/ichi_bns.c:7412..7464,7466..7510` holds old/new vertex and edge pointers, directly decrements or restores individual flow/cap/adjacency fields, and `memset`s one removed edge or vertex in each branch; no whole existing record copy is made for field changes. | Rust `crates/cosmolkit-inchi/src/source/base/ichi_bns.rs:8336..8425` clones each new vertex, edge and old vertex, writes full old vertex and edge records for every removed temporary edge, then clones and writes full old vertex/edge records for cap restoration in either branch; `bns_vertex`/`bns_edge` at `:1114..1134` clone entire records and mutable helpers resolve arena records. | Both paths remain O(number of removed edges plus restored caps) with O(1) local records and no body allocation, but Rust makes multiple full-record copies and writes per edge/cap instead of direct source field updates. Source-defined values are analyzed separately; this is concrete possible memory-traffic and hot-path overhead beyond the shared one-access lookup findings 0033/0034. | Alternating-path restoration callers. | Direct `panic!` at the selected `bSetNew` and nonzero `bSetOldCapsVert` gates in each of the two `bChangeFlow` branches, before the first BNS record snapshot; existing code stays below all four sites. |
| INCHI-AUDIT-0069 | INCHI-F0167 `RemoveLastGroupFromBnStruct`, each removed group edge | Performance: two whole endpoint-vertex writebacks around the edge-order check | Duplicate full-record write and clone per edge confirmed statically; elapsed impact unmeasured | Pinned C `third_party/InChI/INCHI-1-SRC/INCHI_BASE/src/ichi_bns.c:4030..4064` keeps `vert_endpoint` as a pointer, updates its st cap/flow and type fields in place, checks edge neighbor ordinal, then decrements the endpoint adjacency count and clears the edge; there is no intermediate whole-vertex copy. | Rust `crates/cosmolkit-inchi/src/source/base/ichi_bns.rs:8643..8671` copies `vert_endpoint` through `bns_vertex`, updates fields, clones and writes the complete vertex at `:8665`, checks `neigh_ord`, then writes the complete vertex again at `:8670` after decrementing adjacency. `source_types/generated.rs:2400..2465` defines the full BNS vertex/edge records. | Each valid removed group edge adds one extra full-vertex clone and full-vertex writeback before the ordinal check, then another full writeback after it, where C makes only field writes through a retained pointer. Both remain O(group degree), O(1) local storage and allocation-free, but the duplicate record traffic is a concrete possible hot-path cost. The intermediate write preserves C's partial-update state on the ordinal-error branch; no defined-state behavior difference is claimed. | Group removal callers from hard proton and charge paths. | Direct `panic!` immediately before the intermediate `*bns_vertex_mut(...)=vert_endpoint.clone()` inside the existing per-edge loop; all original writes remain. |
| INCHI-AUDIT-0070 | INCHI-F0170 `AugmentEdge`, valid source/sink and atom-edge updates | Performance: full BNS vertex or edge copy/writeback for source pointer field updates | Whole-record traffic on both valid target categories confirmed statically; elapsed impact unmeasured | Pinned C `third_party/InChI/INCHI-1-SRC/INCHI_BASE/src/ichi_bns.c:9947..10040` receives a `BNS_ST_EDGE *` for source/sink cases and changes flow/pass fields in place; `:10043..10086` receives a `BNS_EDGE *` for atom edges and changes its flow/pass fields in place, with direct alt-path array writes. | Rust `crates/cosmolkit-inchi/src/source/base/ichi_bns.rs:9043..9075` clones a complete BNS vertex, updates its st-edge, and `clone_from`s the whole vertex back for a valid source/sink update; `:9172..9199` clones and writes back a complete BNS edge for atom-edge updates; `:9080..9163,9201..9256` additionally resolves, clones and writes whole `BNS_ALT_PATH` entries for scalar path fields. Record layouts are in `source_types/generated.rs:2400..2466`; shared lookup costs are 0032/0033/0034. | Every valid augmenting edge visit copies and writes a complete vertex or edge instead of directly updating the relevant flow/pass fields; source/sink and atom-edge paths can be visited repeatedly in the augmenting-path algorithm. Both remain O(1) per visit with O(1) local state and no body allocation, but full-record traffic plus repeated virtual path-entry resolutions is a concrete possible hot-path cost. No valid-state value divergence is claimed. | Bond-network augmenting-path search callers. | Two direct `panic!` sites immediately before the source/sink `bns_vertex` snapshot and atom-edge `bns_edge` snapshot after the valid pointer-category gate; original code remains below. |
| INCHI-AUDIT-0071 | INCHI-F0172 `rescap_mark`, first visit to a source/sink or atom edge | Performance: complete vertex or edge writeback to set one path bit | Whole-record copy/writeback on first-visit mark confirmed statically; elapsed impact unmeasured | Pinned C `third_party/InChI/INCHI-1-SRC/INCHI_BASE/src/ichi_bns.c:10113..10148` reads the selected st-edge or ordinary edge through a pointer and, on its first path visit, ORs `EDGE_FLOW_ST_PATH` or `EDGE_FLOW_PATH` into that one `flow` field. Second visits only increment `bNotASimplePath` and halve residual capacity. | Rust `crates/cosmolkit-inchi/src/source/base/ichi_bns.rs:9418..9455` first clones a whole BNS vertex or edge through `bns_vertex`/`bns_edge`, then on first visit `clone_from`s the entire updated vertex or edge back through a separately resolved arena slice to set the one path bit. | The first mark of each valid traversed edge adds a full-record copy and writeback plus arena resolution where C performs one field OR through its pointer. Both remain O(1) per visited edge, O(1) local state and allocation-free, but repeated marking in the path search makes the extra record traffic a concrete possible hot-path cost. Reads on second visits inherit shared 0034. No valid-state behavior difference is claimed. | Bond-network residual-capacity path search callers. | Two direct `panic!` sites inside the first-visit `else` blocks before the `flow |= PATH` lines, one for st-edge and one for ordinary edge; original mutation remains below. |
