# Archived Port Plans

These plans have no executable unchecked `Step` entries. Archive placement
does not assert blanket feature completeness or parity beyond each plan's
recorded scope; it only means the document is no longer an active queue.
