[package]
name = "cosmolkit-test-support"
description = "Repository-local test data and expected-output support for COSMolKit"
version.workspace = true
edition.workspace = true
license.workspace = true
publish = false

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sha2 = "0.10"
