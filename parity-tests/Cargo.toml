[package]
name = "cosmolkit-parity-tests"
version = "0.1.0"
edition.workspace = true
publish = false

[dependencies]
cosmolkit = { path = "../crates/cosmolkit", default-features = false, features = ["full"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sha2 = "0.11"

tempfile = "3"
